# RESEARCH REQUEST · 一张表：Effect 预算怎么变成蛊的实际数值

```text
CURRENT PHASE: P1/P2/P2.1/P3-A 已交付。P3-B（Effect 管线数值）卡在这张表。
TYPE: 数值裁决请求（填表即可，不需要重新调研）
FROM: L2 Orchestrator    TO: L1 Research
BLOCKING: P3-B 全部实现。Worker 与 L2 均无权自定这些数。
```

> 同日长稿的精简重写版；长稿证据已入库 `game/world-model/reports/effect-budget-census.md`。

---

## 1. 请你填这张表（本件的全部诉求）

你已冻结：管线形状（D6）、判据（D7）、**预算曲线 40 / 80 / 160 / 320 / 640**（D2）。
缺的只有一步——**把预算数字变成蛊身上可以写的数值。**

| 原型（＝现有 role，覆盖 802 只蛊的 100%） | 现状 r1→r5 | ① 主维度 | ② 主维度**预算占比** | ③ 主维度**一转基准** | ④ 其余预算给 D7 的哪几项 |
|---|---|---|---|---|---|
| attack（379 只） | 2→6 | 伤害 `amount` | ? | ? | ? |
| defense（100 只） | 3→7 | 护盾 `amount` | ? | ? | ? |
| healing（80 只） | 2→6 | 治疗 `amount` | ? | ? | ? |
| logistics（73 只） | 1→5 | 治疗 `amount` | ? | ? | ? |
| movement（92 只） | **1→1** | 位移 `shift` | ? | ? | ? |
| recon（78 只） | **1→1** | 标记 `status` | ? | ? | ? |

- **②③ 定了**，每原型每转的数值就唯一确定：
  `主维度(r) = ③ × ② × 预算(r)/预算(1)`。**取整规则也请给**（四舍五入／向上／下限 1）。
- **④ 留空 = 预算全给主维度**，即五转主维度恰好是一转的 **16 倍**。
  若要让预算花到范围 / 穿透 / 控制 / 资源效率等维度上，就必须在这里写分配比例。

## 2. 填表要用的三个事实（全部实测）

**① 现状兜底曲线只有 3 倍，不是 16 倍。** 741 只（92%）无手写效果，走 role 兜底表：

```
attack  strike  2,3,4,5,6  = 3.0x      Rank Power Budget（你冻结的）
defense shield  3,4,5,6,7  = 2.3x        40, 80, 160, 320, 640 = 16.0x
healing heal    2,3,4,5,6  = 3.0x
logistics heal  1,2,3,4,5  = 5.0x
movement shift  1,1,1,1,1  = 1.0x   ← 完全不随转
recon   status  1,1,1,1,1  = 1.0x   ← 完全不随转
```

**② D7 列的 11 个可花维度里，现行只用了 1 个（`amount`）。** 所以「预算 16 倍」目前无处可去。

**③ 量纲缺口：预算 40 与主维度现状值 2 差 20 倍，来源不明。**
锚点：徒手 `100 × unarmed_damage_ratio 0.2 = 20`；常见敌人气血 4；
`rank1_budget = 徒手 20 × rank_step_ratio 2 = 40`。**表里的 ③ 就是在补这个缺口。**

## 3. 另外两行（可默认，不回答我也能开工）

1. **原型清单**：沿用现有 6 个 role + 现有 8 个 `kind`，不重划 —— 可以吗？
2. **倒挂判据**：D7 的「同定位、同代价结构」要写成可执行断言（R5）。
   建议：按 `(role, kind)` 分组，再按 `value` / `essence_cost` 分档，
   要求「高档位的能力预算不低于低档位」。**分档口径由你定，我不自行发明。**

> 背景（一句话）：三转 180 只蛊仅 10 只手写，纯攻击只有 1 只且 amount=2，低于一转/二转的 4 —— 按 D7 是真倒挂。

## 4. 约束

- 本件未回复前 **P3-B 不动**；P3-A（普查）与旁支补线（敌人运行时）不受影响，已并行。
- 不必迁就现有 `amount` 量级——那批数字本就是未论证的字面量，整体重标定也可以，
  只要 ③ 给出，我就按新口径改。
- D7 那句「一转纯攻击蛊伤害 4、三转侦查/控制蛊伤害 2 不叫倒挂」我完全接受；
  只担心同一 `(role, kind)` 内高转不如低转。
