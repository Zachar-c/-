# 视觉素材 v2 · 子包 2a：青茅山 + 商家城·南疆（L3，READ ONLY）

> ## ⛔ 本包已作废（VOID，2026-09-20），不得再派发
>
> **作废依据**：本包给了 24 窗口（A+B 两阶段）约 **500 KB 中文原文**一次读完。
> Worker **真的读完了全部 24 窗**（方法正确，公共约定生效），但上下文耗尽，
> **交付物零字节**——`visual-library-v2a.md` 与 `visual-v2a-result.md` 均未生成就退出。
>
> **根因（在 L2）**：违反了自己记下的「单次派发 ≤1 阶段（~250KB）」，一次给了两个阶段。
> 前一次预判的「覆盖不足」方向是错的——不是读不完，是**读完就没余量写了**。
>
> **替代包**：`visual-v2a1-qingmao.md`（仅 A 阶段，12 窗 × 150 行，**强制边读边写**）
> 及其后续 `2a2`（商家城·南疆）/ `2b`（中洲）/ `2c`（北原西漠东海+后期）。
> 本包保留在库内仅作留痕。

---

> **第一步：先读 `ai-system/tasks/visual-v2-schema.md`（公共约定）。本件只写本子包的差异部分。**
> 出处：L1 Product Research《问真》视觉素材二次采集任务 v2。本包是 L2 重写后的三个子包之一。

```text
WORKFLOW ROLE
L3 Visual Lore Research Worker

UPSTREAM
L1 Product Research（任务书）→ L2 Orchestrator（本包）

DOWNSTREAM
L2 复核 → 子包 2b（中洲）/ 2c（北原西漠东海+后期）→ L2 集成 → L1 裁决 → L0 批准

MODE
READ ONLY

PROJECT GOAL
把《蛊真人》做成游戏（《问真》）。PRD 定位「以世界运行逻辑为灵感的策略构筑游戏」，
核心幻想「我是一名真正的蛊师」。L0 已禁止直接继承现有水墨方案，要求重研原著后提方案。

CURRENT PHASE
V2 需求重构阶段，**禁止开发**。本任务不写代码、不改数据、不建资产、不做设计。

TASK PURPOSE
建立《蛊真人》视觉素材数据库，为 L1 制定视觉方向提供事实基础。
本子包负责**最早的两个人间阶段**：青茅山的低阶蛊师世界，与商家城·南疆的资源社会。
它们是全书视觉的"地面基准"——后面所有超凡尺度都从这里长出去。

TASK

## 一、本子包的采集范围

| 代号 | 阶段 | 定位 | 窗口数 | 对象配额 |
|---|---|---|---|---|
| **A** | 青茅山阶段（低阶蛊师世界） | 行号 00001–45000，十二等分取中点±150 | 12 | **≥18** |
| **B** | 商家城 / 南疆阶段（资源社会） | 行号 45001–135000，十二等分取中点±150 | 12 | **≥16** |

**每窗读 300 行**，合计 24 窗 ≈ 7,200 行。

**A 的采集重点**（L1 指定）：山寨 / 家族 / 学堂 / 元泉 / 蛊虫 / 一转炼蛊 / 凡人生活
**B 的采集重点**（L1 指定）：商队 / **坊市** / 拍卖 / 炼蛊师 / 传承 / 战斗

> **省你时间的已核事实**：`坊市` 在全书 **0 命中**（L2 实测）。不要去找它。
> 原著实际的商业载体是：**学堂铺**（`学堂` 441 次集中在 A 段）、**酒楼**、**商队**（`商队` 411 次，A 段 228）、
> **拍卖**（`拍卖` 320 次，B 段 83）。请采集这些真实存在的形态。

## 二、对象格式

**严格按 `visual-v2-schema.md` §二**（对象名称 / 原文位置 / 原文摘录【事实】/ 视觉转述【事实】/ 视觉关键词【转译】/ 价值评级【评级】）。

重申两条最容易犯的：

- **视觉转述是一段连贯的话**，不是 6 个槽位。原文没提供的角度就不要写。**禁止脚本切片。**
- **对象名必须是原文的具体名字**。❌「蛊虫」「二转蛊师」「年青蛊师」「斗蛊」❌任何 `·行N` 形式。

## 三、自查（交付前必跑，结果写进结果包）

按 `visual-v2-schema.md` §三，另加：

- 本子包对象总数 ≥34，且 A/B 两阶段**各自**达标（A≥18 / B≥16）

SCOPE
只读：`蛊真人-clean.txt`（仓库根，未跟踪）、`lore/wiki/**`（仅导航）、
`ai-system/tasks/visual-archaeology-corpus.md`（上轮产物，**只读参考不得抄写**）

可写（**仅这两个**）：
- `ai-system/tasks/visual-library-v2a.md`（本子包素材库 + 采样栅格）
- `ai-system/tasks/visual-v2a-result.md`（Caveman Review Packet）

DO NOT
见 `visual-v2-schema.md` §六（全部适用）。额外强调：

- **禁止用脚本生成对象条目**——上一版正是这样失败的
- **禁止用别阶段的对象凑本子包配额**

DECISION AUTHORITY
可自行决定：窗口内的具体取点、对象颗粒度与命名、关键词措辞、★ 评级判断（附理由）。
**不可自行决定**：视觉方向、美术风格、任何"应该做成什么样"的结论。

ESCALATE WHEN
- A 或 B 在 12 窗内达不到配额（**如实上报，不要凑数**）
- 某窗口找不到真正的视觉对象（**记 0，不要抓无关句子**）
- 原文规模/结构与公共约定给出的数字对不上

DELIVERABLE
1. `ai-system/tasks/visual-library-v2a.md`：素材库（≥34 对象）+ 采样栅格（24 窗，允许 0）
2. `ai-system/tasks/visual-v2a-result.md`：Caveman Review Packet（追加 FACT/ANALYSIS/UNCHECKED）

**预算不足时**：先保**对象真实与字段语义正确**，配额可让步，**如实标 PARTIAL**。

ACCEPTANCE
- A≥18 / B≥16，合计 ≥34 对象
- 每个对象带行号 + ≥1 条 ≤60 字短引，可由 L2 用 grep 复核
- **无占位名、无跨字段重复、无脚本切片痕迹**
- 三层标注齐全；「视觉转述」是连贯语句而非槽位填充
- 采样栅格齐全（允许窗口 0 对象）
- 无任何画风结论；无「适合做成」
- `git status --porcelain` 只显示本 SCOPE 内两个文件

STATUS TARGET
READY_FOR_L2_REVIEW
```

## 执行摘要（Worker 请照此报数）

- 窗口：A 12 窗 + B 12 窗 = 24 窗
- 配额：A≥18，B≥16，合计 ≥34
- 自查：占位名 0 ／ 重复字段 0 ／ 短引 100% 命中 ／ 层标注齐全 ／ 风格词 0
