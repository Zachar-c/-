# 视觉素材 v2 · 子包 2c1：北原 / 西漠 / 东海（L3，READ ONLY）

> **第一步：先读 `ai-system/tasks/visual-v2-schema.md`（公共约定）。本件只写差异部分。**
> 模板来自子包 2a1（已通过 L2 复核），**参数照搬**。
> 原设计把 D+E 合成一个「2c」（27 窗），**已拆开**——27 窗正是杀死子包 2a 的过量。

```text
WORKFLOW ROLE
L3 Visual Lore Research Worker

UPSTREAM
L1 Product Research（任务书）→ L2 Orchestrator（本包）

DOWNSTREAM
L2 复核 → L2 集成（与 2a1/2a2/2b/2c2 合并）→ L1 裁决

MODE
READ ONLY

PROJECT GOAL
把《蛊真人》做成游戏（《问真》）。PRD 定位「以世界运行逻辑为灵感的策略构筑游戏」，
核心幻想「我是一名真正的蛊师」。L0 已禁止直接继承现有水墨方案，要求重研原著后提方案。

CURRENT PHASE
V2 需求重构阶段，**禁止开发**。本任务不写代码、不改数据、不建资产、不做设计。

TASK PURPOSE
建立《蛊真人》视觉素材数据库。本子包负责**三个不同文明形态的地域**：
北原、西漠、东海。L1 给这一段的指示是：

> 「重点：不是地图。而是：为什么这些地方看起来应该不同？」

**本批只做采集**：把每个地域**有辨识度**的视觉对象带地域标签收集起来。
「为什么不同」的对比分析属批次 2（专题），**本批不得下结论**。

TASK

## 一、采集范围（**地域簇，不是时间段**）

实测这三个词**遍布全书且互相重叠**（`北原` 2404 次、`西漠` 813 次、`东海` 1611 次），
所以**不能用时间区间采样**——必须用**该地域词命中分布的五等分位当锚点**，
保证窗口落在真正在写该地域的文字里。

**每锚点取 1 个窗口，每窗连续读 120 行。15 个锚点 = 15 窗 = 1,800 行。**

| 地域 | 锚点（该词命中分布五等分位，每点起读 120 行） |
|---|---|
| **北原** | 100754 · 137238 · 174300 · 226054 · 318984 |
| **西漠** | 240892 · 278894 · 329300 · 358666 · 404816 |
| **东海** | 168816 · 241498 · 316540 · 350656 · 388912 |

**配额：≥20 个对象**（三个地域都要有，不得全部来自同一地域）。

> 算式说明：15 窗 × 120 行 = **1,800 行**，与已验证可用的 2a1 配方
> 「12 窗 × 150 行 = 1,800 行」**等量**。**不要自行加窗或加行数。**

## 二、对象格式

**严格按 `visual-v2-schema.md` §二**：

```text
对象名称：<原文里的具体名字>
原文位置：行号 <N>
视觉转述【事实】：<≤150 字，一段连贯的话>
   （含 原文摘录【事实】：行号 <N>：「<≤60 字短引>」）
视觉关键词【转译】：<3–8 个>
价值评级【评级】：<★1–5> —— <一句话理由>
```

**本包额外要求**：每个对象在「原文位置」后**必须标出它属于哪个地域**
（北原 / 西漠 / 东海 / 其他），格式：`原文位置：行号 <N>（地域：北原）`。
这是给批次 2 做地域对比用的，**不要省略**。

三条最易犯错的：

- **视觉转述是一段连贯的话**。原文没写的就不写。**禁止切片填栏，禁止脚本生成条目。**
- **对象名必须是原文的具体名字**。❌泛称 ❌任何 `·行N` 形式。
- **允许某窗口 0 对象**，如实记 0。**不要为凑配额抓无关句子。**

## 三、执行顺序（**照此做，不要改**）

```text
1. 写输出文件头（标题 + 采样栅格表骨架，15 行待填）
2. for 窗口 D01..D15（按上表顺序，北原5 → 西漠5 → 东海5）:
     a. 读该窗 120 行
     b. 就地判断：有哪些"视觉对象"？（0–3 个）
     c. 立即把对象条目【追加】写入 visual-library-v2c1.md
     d. 回到步骤 2 处理下一窗
3. 回填栅格表的「采到对象数」列
4. 跑自查，写结果包
```

**第 2c 步的「立即追加」是本包的核心**（已在 2a1/2a2 验证有效）。不要攒到最后才写。

SCOPE
只读：`蛊真人-clean.txt`、`lore/wiki/**`（仅导航）、
`ai-system/tasks/visual-archaeology-corpus.md` 及 `visual-library-v2a1.md` / `v2a2.md` / `v2b.md`
（**同批前作，只读参考，不得抄写**）

**窗口自建，用自己的临时目录**：
`C:\Users\90877\AppData\Local\Temp\opencode\w2c1\*.txt`（带行号前缀 `行号: 正文`）

可写（**仅这两个**）：
- `ai-system/tasks/visual-library-v2c1.md`（素材库 + 采样栅格）
- `ai-system/tasks/visual-v2c1-result.md`（Caveman Review Packet）

> **并行提示**：可能有其他子包 Worker 同时运行。**只动你自己的文件与 `w2c1/` 目录**，
> 不要碰 `w2a/` `w2b/` 或其他 `visual-library-v2*.md`。

DO NOT
见 `visual-v2-schema.md` §六。额外强调：

- **禁止用脚本生成对象条目**（脚本只可用于定位与自查）
- **禁止一次读完所有窗口再写**——必须边读边写
- **禁止自行扩大窗口数或行数**
- **禁止下「三地为何不同」的结论**——那是批次 2 的分析任务

DECISION AUTHORITY
可自行决定：窗口内的具体取点、对象颗粒度与命名、关键词措辞、★ 评级（附理由）。
**不可自行决定**：视觉方向、美术风格、地域对比结论、任何"应该做成什么样"的判断。

ESCALATE WHEN
- 15 窗读完仍达不到 ≥20 对象（**如实上报，不要凑数**）
- 某锚点窗口内该地域词 **0 命中**（锚点落空，请上报）
- 某地域完全采不到对象（**如实报告，不要用别的地域凑**）

DELIVERABLE
1. `ai-system/tasks/visual-library-v2c1.md`：≥20 对象（带地域标签）+ 采样栅格（15 窗）
2. `ai-system/tasks/visual-v2c1-result.md`：Caveman Review Packet（追加 FACT/ANALYSIS/UNCHECKED）

**预算将尽的处理**：立刻停止采集，保住已落盘内容，写结果包并标 `PARTIAL`。
**绝不允许**为了写完而编造对象或机械切片。

ACCEPTANCE
- ≥20 对象，**三个地域都有覆盖**
- 每个对象带行号 + **地域标签** + ≥1 条 ≤60 字短引，可由 L2 用 grep 复核
- **无占位名、无跨对象重复转述、无脚本切片痕迹**
- 三层标注齐全；「视觉转述」是连贯语句
- 采样栅格齐全（允许窗口 0 对象）
- 无画风结论；无地域对比结论；无「适合做成」
- `git status --porcelain` 只显示本 SCOPE 内两个文件

STATUS TARGET
READY_FOR_L2_REVIEW
```

## 执行摘要（Worker 请照此报数）

- 范围：**仅 D 阶段**，15 窗 × 120 行（合计 1,800 行）
- 锚点：北原 100754/137238/174300/226054/318984；西漠 240892/278894/329300/358666/404816；东海 168816/241498/316540/350656/388912
- 配额：≥20 对象，每窗 ≤3，**三地域均需覆盖**
- 强制：**边读边写** + **每对象标地域**
- 临时目录：`w2c1/`
- 自查：占位名 0 ／ 重复转述 0 ／ 短引 100% 命中 ／ 三层齐全 ／ 风格词 0
