# L3 任务包 · v3-A3 蛊的本体形态（六至九转仙蛊，≥20 只）

```text
WORKFLOW ROLE: L3 Visual Lore Research Worker（READ ONLY 输入 / 只写一个产出文件）
UPSTREAM: Codex Orchestrator（L2）
DOWNSTREAM: Codex Review（L2） → L1（ChatGPT）方向裁决 → L0 批准

PROJECT GOAL:
为《问真》确定视觉设计自由度边界：原著哪些必须忠于，哪些可以自由创造、重新设计。
CURRENT PHASE:
V2 需求重构期。L0 已禁止开发，只允许研究/采集/归纳。

TASK PURPOSE:
L1 v3 任务书 A 节（≥60 只蛊的本体形态）之子包：**六至九转仙蛊 20 只**。
这一段是本轮的对照关键：高阶蛊到底还写不写本体形态？如不写，本子包要如实报告「20 只里有多少只根本没有外形描写」。

TASK:
定向检索原文，取 20 只六转及以上的仙蛊，逐只记录 14 个字段：
蛊名 / 转数 / 流派 / 本体是否直接描写 / 外形 / 材质 / 尺度 /
是否生命化 / 是否器物化 / 是否植物化 / 是否人形拟人 / 是否抽象概念化 /
催动时是否改变形态 / 原文证据（行号 + ≤60 字短引）/ CANON CONSTRAINT（HARD/SOFT/OPEN）。
（「本体是否直接描写」这一字段是本子包的重点，必须逐只明确回答；原文未描写则其余形体字段写「原文未提供」。）

检索线索（自行扩展）：`六转` `七转` `八转` `九转` `仙蛊`；
蛊名构词（X 仙蛊 / X 蛊）；形体词 `外形` `通体` `光芒` `虚影` `形态` `化作`。

SCOPE:
读：根目录 `蛊真人-clean.txt`、`ai-system/tasks/visual-v3-schema.md`（公共约定，必读）。
写：`ai-system/tasks/visual-v3a3-immortal-gu-form.md`（唯一产物）。

DO NOT:
- 不得修改 wiki、game、任何已存在任务文件。
- 不得提画风结论，不得推荐配色、UI、角色造型、美术方案。
- 不得把转述当引文；不得用低转数蛊补足名额。

DECISION AUTHORITY:
选哪 20 只自行判断；须写明选取口径（含转数分布）。

ESCALATE WHEN:
六转以上蛊的形体描写确实稀少（可采 < 20 只）——停手上报实际可采数量与「有外形描写 / 无外形描写」的比例，不要用低转数补足。

DELIVERABLE:
`ai-system/tasks/visual-v3a3-immortal-gu-form.md`：选取口径 / 20 条记录 /
「有外形描写 vs 无外形描写」的比例统计 / A3 段小结 / 自查表。

ACCEPTANCE:
- ≥20 只且全为六转及以上；每条有行号 + ≤60 字原文连续短引。
- 「本体是否直接描写」逐只明确；无描写时其余形体字段写「原文未提供」。
- 无画风词、无美术建议；自查表齐全。

STATUS TARGET: READY_FOR_REVIEW
```

## 返回格式

```text
STATUS: DONE / PARTIAL / BLOCKED
CHANGED:
FOUND:
EVIDENCE:
TESTS:
RISKS:
QUESTIONS:
```
