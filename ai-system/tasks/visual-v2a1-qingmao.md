# 视觉素材 v2 · 子包 2a1：青茅山阶段（L3，READ ONLY）

> **第一步：先读 `ai-system/tasks/visual-v2-schema.md`（公共约定）。本件只写差异部分。**
> 前两次派发（批次1 全量、子包 2a 含 A+B）均已失败，失败原因见下，**本包是为绕开它而设计的**。

```text
WORKFLOW ROLE
L3 Visual Lore Research Worker

UPSTREAM
L1 Product Research（任务书）→ L2 Orchestrator（本包）

DOWNSTREAM
L2 复核 → 子包 2a2（商家城·南疆）→ 2b（中洲）→ 2c（北原西漠东海+后期）→ L2 集成 → L1 裁决

MODE
READ ONLY

PROJECT GOAL
把《蛊真人》做成游戏（《问真》）。PRD 定位「以世界运行逻辑为灵感的策略构筑游戏」，
核心幻想「我是一名真正的蛊师」。L0 已禁止直接继承现有水墨方案，要求重研原著后提方案。

CURRENT PHASE
V2 需求重构阶段，**禁止开发**。本任务不写代码、不改数据、不建资产、不做设计。

TASK PURPOSE
建立《蛊真人》视觉素材数据库。本子包负责**全书最早的地面基准**：
青茅山的低阶蛊师世界。后面所有超凡尺度都从这一层长出去。

## ⚠️ 前两次失败与本包的设计对策

| 次 | 做了什么 | 结果 |
|---|---|---|
| 批次1 | 103 对象 × 6 个语义字段，一次派发 | 配额压垮质量，Worker 改用脚本机械切片 → 对象名成 `蛊虫·行13016`，**REJECT** |
| 子包 2a | A+B 两阶段、24 窗口约 500 KB 原文一次读完 | **真读完了 24 窗**（方法正确），但上下文耗尽，**交付物零字节** |

**对策（本包强制）**：
1. **只做 A 阶段**（12 窗，不是 24 窗）——减半阅读量。
2. **窗口只读 150 行**（中点±75），不是 300 行——再减半。
3. **每读完一个窗口，立即把该窗口采到的对象追加写入输出文件**，不得攒到最后一次性写。
   即使中途预算耗尽，磁盘上也要有已采成果。
4. **每窗最多采 3 个对象**——宁少勿烂。

TASK

## 一、采集范围

| 阶段 | 定位 | 窗口 | 每窗行数 | 对象配额 |
|---|---|---|---|---|
| **A 青茅山阶段** | 行号 00001–45000，十二等分取中点±75 | 12 | 150 | **≥18** |

采集重点：山寨 / 家族 / 学堂 / 元泉 / 蛊虫 / 一转炼蛊 / 凡人生活

**窗口已由前次派发落盘**，可直接复用（若不存在再自行生成）：
`C:\Users\90877\AppData\Local\Temp\opencode\w2a\A01.txt … A12.txt`（每窗 300 行，带行号前缀）
→ **你只读每窗的前 150 行即可**（配额与定位按此算）。

## 二、对象格式

**严格按 `visual-v2-schema.md` §二**：

```text
对象名称：<原文里的具体名字>
原文位置：行号 <N>
原文摘录【事实】：
    行号 <N>：「<≤60 字短引>」
视觉转述【事实】：<≤150 字，一段连贯的话>
视觉关键词【转译】：<3–8 个>
价值评级【评级】：<★1–5> —— <一句话理由>
```

三条最易犯错的，重申：

- **视觉转述是一段连贯的话**，用你自己的话复述原文对该对象的视觉呈现。原文没写的就不写。
  **禁止把同一句切片填进多栏**，**禁止用脚本生成条目**。
- **对象名必须是原文的具体名字**。❌「蛊虫」「二转蛊师」「年青蛊师」「斗蛊」❌任何 `·行N` 形式。
- **允许某窗口 0 对象**，如实记 0。**不要为凑配额抓无关句子。**

## 三、执行顺序（**照此做，不要改**）

```text
1. 写输出文件头（标题 + 采样栅格表骨架，12 行待填）
2. for 窗口 A01..A12:
     a. 读该窗 150 行
     b. 就地判断：有哪些"视觉对象"？（0–3 个）
     c. 立即把对象条目【追加】写入 visual-library-v2a1.md
     d. 回到步骤 2 处理下一窗
3. 回填栅格表的「采到对象数」列
4. 跑自查，写结果包
```

**第 2c 步的「立即追加」是本包的核心**。不要在内存里攒对象清单到最后才写。
每次只处理当前窗口，写完就丢。

SCOPE
只读：`蛊真人-clean.txt`、`lore/wiki/**`（仅导航）、
`ai-system/tasks/visual-archaeology-corpus.md`（上轮只读参考，不得抄写）、
`C:\Users\90877\AppData\Local\Temp\opencode\w2a\*.txt`（已落盘窗口）

可写（**仅这两个**）：
- `ai-system/tasks/visual-library-v2a1.md`（素材库 + 采样栅格）
- `ai-system/tasks/visual-v2a1-result.md`（Caveman Review Packet）

DO NOT
见 `visual-v2-schema.md` §六（全部适用）。额外强调：

- **禁止用脚本生成对象条目**（脚本只可用于定位与自查）
- **禁止一次读完所有窗口再写**——必须边读边写（见 §三）
- 禁止用别阶段对象凑本包配额

DECISION AUTHORITY
可自行决定：窗口内的具体取点、对象颗粒度与命名、关键词措辞、★ 评级（附理由）。
**不可自行决定**：视觉方向、美术风格、任何"应该做成什么样"的结论。

ESCALATE WHEN
- 12 窗读完仍达不到 ≥18 对象（**如实上报，不要凑数**）
- 某窗口找不到视觉对象（**记 0**）
- 原文规模/结构与公共约定给出的数字对不上

DELIVERABLE
1. `ai-system/tasks/visual-library-v2a1.md`：≥18 对象 + 采样栅格（12 窗，允许 0）
2. `ai-system/tasks/visual-v2a1-result.md`：Caveman Review Packet（追加 FACT/ANALYSIS/UNCHECKED）

**预算将尽的处理**：立刻停止采集，**把已落盘的内容保留**，写结果包并标 `PARTIAL`。
**绝不允许**为了写完而编造对象或机械切片——那正是批次 1 被 REJECT 的原因。

ACCEPTANCE
- ≥18 对象
- 每个对象带行号 + ≥1 条 ≤60 字短引，可由 L2 用 grep 复核
- **无占位名、无跨对象重复转述、无脚本切片痕迹**
- 三层标注齐全；「视觉转述」是连贯语句
- 采样栅格齐全（允许窗口 0 对象）
- 无画风结论；无「适合做成」
- `git status --porcelain` 只显示本 SCOPE 内两个文件

STATUS TARGET
READY_FOR_L2_REVIEW
```

## 执行摘要（Worker 请照此报数）

- 范围：**仅 A 阶段**，12 窗 × 150 行
- 配额：≥18 对象，每窗 ≤3
- 强制：**边读边写**（读完一窗立即追加）
- 自查：占位名 0 ／ 重复转述 0 ／ 短引 100% 命中 ／ 三层齐全 ／ 风格词 0
