# 视觉素材 v2 · 补采包 2d：东海（L3，READ ONLY）

> **第一步：先读 `ai-system/tasks/visual-v2-schema.md`（公共约定）。本件只写差异部分。**
> 模板来自子包 2a1（已通过 L2 复核），**参数照搬**。

```text
WORKFLOW ROLE
L3 Visual Lore Research Worker

UPSTREAM
L1 Product Research（任务书）→ L2 Orchestrator（本包）

DOWNSTREAM
L2 复核 → L2 集成（与 2a1/2a2/2b/2c1/2c2 合并去重）→ L1 裁决

MODE
READ ONLY

PROJECT GOAL
把《蛊真人》做成游戏（《问真》）。PRD 定位「以世界运行逻辑为灵感的策略构筑游戏」，
核心幻想「我是一名真正的蛊师」。L0 已禁止直接继承现有水墨方案，要求重研原著后提方案。

CURRENT PHASE
V2 需求重构阶段，**禁止开发**。本任务不写代码、不改数据、不建资产、不做设计。

TASK PURPOSE
**这是一个补采包。** 前一批 2c1（北原/西漠/东海）采到 33 个对象，但地域分布失衡：

| 地域 | 对象数 | 该词全书命中 |
|---|---|---|
| 北原 | 9 | 2404 |
| 西漠 | 10 | 813 |
| **东海** | **3** | **1611** |

东海的原著命中数**比西漠还多一倍**，却只采到 3 个——这是**采样偏差**（2c1 的五个五等分位锚点
落在了视觉稀疏的段落），不是原著本身贫瘠。本包专补东海，把三个地域拉平，
否则 L1 做「为什么这些地方看起来应该不同」时东海那一格是空的。

TASK

## 一、采集范围（**地域簇，用分位锚点**）

**本包全部锚点都在东海**，共 **12 窗 × 150 行 = 1,800 行**（与已验证配方等量）。

**① `东海` 十分位锚点（比 2c1 的五等分位更密）——每点 1 窗，共 10 窗：**

```text
149330 · 170388 · 216166 · 246568 · 311954
340950 · 350198 · 359512 · 386012 · 405930
```

**② 两个东海强实体锚点——各 1 窗，共 2 窗：**

| 锚点 | 行号 | 理由 |
|---|---|---|
| `龙宫` 命中中位数 | **371542** | `龙宫` 全书 **831** 命中，东海最强视觉实体 |
| `鲛人` 命中中位数 | **308706** | `鲛人` 全书 **460** 命中，东海独有物种 |

**配额：≥12 个对象。**

> **已核实的真实靶子**（L2 实测全书命中）：`龙宫` **831** ／ `鲛人` **460** ／
> `海底` 258 ／ `海面` 192 ／ `大海` 55 ／ `海浪` 45 ／ `珊瑚` 39 ／ `礁` 31。
>
> **陷阱词**：`水府` 全书 **0 命中**——不要去找它。（前几批已踩过 `坊市` 0、`宗门` 5 两个同类坑。）

> **已采过的 3 个东海对象**（2c1 产出）：`苍蓝龙鲸` / `蓝鳞海龙` / `龙宫`。
> 本包**不必刻意重复**它们；若你在窗口里遇到同一对象的**更好视觉描写**，照常采，由 L2 在集成时去重。

## 二、对象格式

**严格按 `visual-v2-schema.md` §二**：

```text
对象名称：<原文里的具体名字>
原文位置：行号 <N>（地域：东海）
原文摘录【事实】：
    行号 <N>：「<≤60 字短引>」
视觉转述【事实】：<≤150 字，一段连贯的话>
视觉关键词【转译】：<3–8 个>
价值评级【评级】：<★1–5> —— <一句话理由>
```

**地域标签 `（地域：东海）` 必填**（与 2c1 同格式），便于集成时按地域归并。

三条最易犯错的：

- **视觉转述是一段连贯的话**。原文没写的就不写。**禁止切片填栏，禁止脚本生成条目。**
- **对象名必须是原文的具体名字**。具名个体/地点/器物/生物/天象都算；
  ❌泛称分类（「蛊师」「鱼群」）❌任何 `·行N` 占位形式。
- **允许某窗口 0 对象**，如实记 0。**不要为凑配额抓无关句子。**

## 三、执行顺序（**照此做，不要改**）

```text
1. 写输出文件头（标题 + 采样栅格表骨架，12 行待填）
2. for 窗口 E01..E12（①的10窗 → ②的2窗）:
     a. 读该窗 150 行
     b. 就地判断：有哪些"视觉对象"？（0–3 个）
     c. 立即把对象条目【追加】写入 visual-library-v2d.md
     d. 回到步骤 2 处理下一窗
3. 回填栅格表的「采到对象数」列
4. 跑自查，写结果包
```

**第 2c 步的「立即追加」是本包的核心**（已在 2a1/2a2/2b/2c1/2c2 验证有效）。不要攒到最后才写。

SCOPE
只读：`蛊真人-clean.txt`、`lore/wiki/**`（仅导航）、
`ai-system/tasks/visual-library-v2*.md`（**同批前作，只读参考，不得抄写**）

**窗口自建，用自己的临时目录**：
`C:\Users\90877\AppData\Local\Temp\opencode\w2d\*.txt`（带行号前缀 `行号: 正文`）

可写（**仅这两个**）：
- `ai-system/tasks/visual-library-v2d.md`（素材库 + 采样栅格）
- `ai-system/tasks/visual-v2d-result.md`（Caveman Review Packet）

> **并行提示**：可能有其他 Worker 在跑。**只动你自己的文件与 `w2d/` 目录**，
> 不要碰 `w2a/` `w2b/` `w2c1/` `w2c2/` 或其他 `visual-library-v2*.md`。

DO NOT
见 `visual-v2-schema.md` §六。额外强调：

- **禁止用脚本生成对象条目**（脚本只可用于定位与自查）
- **禁止一次读完所有窗口再写**——必须边读边写
- **禁止自行扩大窗口数或行数**
- **禁止用其他地域的对象凑本包配额**

DECISION AUTHORITY
可自行决定：窗口内的具体取点、对象颗粒度与命名、关键词措辞、★ 评级（附理由）。
**不可自行决定**：视觉方向、美术风格、任何"应该做成什么样"的结论。

ESCALATE WHEN
- 12 窗读完仍达不到 ≥12 对象（**如实上报，不要凑数**）
- 某锚点窗口内 `东海` 0 命中（锚点落空，请上报）
- 东海在原文中确实视觉稀薄（**如实报告，这是有价值的发现**，不要填坑）

DELIVERABLE
1. `ai-system/tasks/visual-library-v2d.md`：≥12 对象（带「地域：东海」标签）+ 采样栅格（12 窗）
2. `ai-system/tasks/visual-v2d-result.md`：Caveman Review Packet（追加 FACT/ANALYSIS/UNCHECKED）

**预算将尽的处理**：立刻停止采集，保住已落盘内容，写结果包并标 `PARTIAL`。
**绝不允许**为了写完而编造对象或机械切片。

ACCEPTANCE
- ≥12 对象，每个带 `（地域：东海）` 标签
- 每个对象带行号 + ≥1 条 ≤60 字短引，可由 L2 用 grep 复核
- **无占位名、无跨对象重复转述、无脚本切片痕迹**
- 三层标注齐全；「视觉转述」是连贯语句
- 采样栅格齐全（允许窗口 0 对象，如实记 0）
- 无画风结论；无「适合做成」
- `git status --porcelain` 只显示本 SCOPE 内两个文件

STATUS TARGET
READY_FOR_L2_REVIEW
```

## 执行摘要（Worker 请照此报数）

- 范围：**仅东海**，12 窗 × 150 行（合计 1,800 行）
- 锚点：`东海` 十分位 149330/170388/216166/246568/311954/340950/350198/359512/386012/405930 ＋ `龙宫` 中位 371542 ＋ `鲛人` 中位 308706
- 配额：≥12 对象，每窗 ≤3
- 强制：**边读边写** + **每对象标「地域：东海」**
- 陷阱词：`水府` 0 命中，不要找
- 临时目录：`w2d/`
- 自查：占位名 0 ／ 重复转述 0 ／ 短引 100% 命中 ／ 三层齐全 ／ 风格词 0
