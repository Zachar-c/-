# 视觉素材 v2 · 子包 2c2：后期尊者 / 天道 / 人道（L3，READ ONLY）

> **第一步：先读 `ai-system/tasks/visual-v2-schema.md`（公共约定）。本件只写差异部分。**
> 模板来自子包 2a1（已通过 L2 复核），**参数照搬**。
> 原设计把 D+E 合成一个「2c」（27 窗），**已拆开**——27 窗正是杀死子包 2a 的过量。

```text
WORKFLOW ROLE
L3 Visual Lore Research Worker

UPSTREAM
L1 Product Research（任务书）→ L2 Orchestrator（本包）

DOWNSTREAM
L2 复核（本包是五阶段中最后一块）→ L2 集成 → L1 裁决

MODE
READ ONLY

PROJECT GOAL
把《蛊真人》做成游戏（《问真》）。PRD 定位「以世界运行逻辑为灵感的策略构筑游戏」，
核心幻想「我是一名真正的蛊师」。L0 已禁止直接继承现有水墨方案，要求重研原著后提方案。

CURRENT PHASE
V2 需求重构阶段，**禁止开发**。本任务不写代码、不改数据、不建资产、不做设计。

TASK PURPOSE
建立《蛊真人》视觉素材数据库。本子包负责**全书最高层的力量与规则表现**：
后期尊者、天道、人道阶段。L1 的指示是「重点：最高层力量表现」。
这一层是尺度轴的顶端——它决定了这套视觉语汇的**上限在哪**。

TASK

## 一、采集范围（**这一阶段是时间段**）

| 阶段 | 定位 | 窗口 | 每窗行数 | 对象配额 |
|---|---|---|---|---|
| **E 后期尊者 / 天道 / 人道** | 行号 360001–437061，十二等分取中点±75 | 12 | 150 | **≥20** |

**配额：≥20 个对象。**

> 算式说明：12 窗 × 150 行 = **1,800 行**，与已验证可用的 2a1 配方**完全等量**。
> **不要自行加窗或加行数。**

**采集重点**（L1 指定）：尊者出场 / 九转力量 / 天道显化 / 人道杀招 / 光阴长河 / 命运相关

> **已核实的真实靶子**（L2 实测全书命中，**本阶段无陷阱词，全部可用**）：
> `仙尊` **3328** ／ `宿命` 1181 ／ `九转` 948 ／ `尊者` 900 ／
> `天道` 592 ／ `光阴长河` 586 ／ `人道` 526 ／ `命运` 267。
> （对照：前两批的 `坊市` 0 命中、`宗门` 5 命中是陷阱词，本阶段没有这种坑。）

## 二、对象格式

**严格按 `visual-v2-schema.md` §二**：

```text
对象名称：<原文里的具体名字>
原文位置：行号 <N>
原文摘录【事实】：
    行号 <N>：「<≤60 字短引>」
视觉转述【事实】：<≤150 字，一段连贯的话>
视觉关键词【转译】：<3–8 个>
价值评级【评级】：<★1–5> —— <一句话理由>
```

**本包特别注意**：后期大量内容是**规则/机制层**（宿命、道痕、气运、命运），
它们不一定是"看得见的东西"。处理原则：

- 若原文给了**视觉呈现**（显化的光、崩解的物象、大阵、长河）→ 照常采。
- 若原文只在**说规则**、没有任何视觉描写 → **不要采**，也不要脑补画面。
  宁可该窗记 0，也不要为填配额把抽象议论包装成"视觉对象"。
- **抽象概念不算视觉对象**，除非原文确实把它画了出来。

三条最易犯错的：

- **视觉转述是一段连贯的话**。原文没写的就不写。**禁止切片填栏，禁止脚本生成条目。**
- **对象名必须是原文的具体名字**。❌「天道」这类泛称除非原文专指具体物 ❌任何 `·行N` 形式。
- **允许某窗口 0 对象**，如实记 0。**不要为凑配额抓无关句子。**

## 三、执行顺序（**照此做，不要改**）

```text
1. 写输出文件头（标题 + 采样栅格表骨架，12 行待填）
2. for 窗口 E01..E12:
     a. 读该窗 150 行
     b. 就地判断：有哪些"视觉对象"？（0–3 个）
     c. 立即把对象条目【追加】写入 visual-library-v2c2.md
     d. 回到步骤 2 处理下一窗
3. 回填栅格表的「采到对象数」列
4. 跑自查，写结果包
```

**第 2c 步的「立即追加」是本包的核心**（已在 2a1/2a2 验证有效）。不要攒到最后才写。

SCOPE
只读：`蛊真人-clean.txt`、`lore/wiki/**`（仅导航）、
`ai-system/tasks/visual-archaeology-corpus.md` 及 `visual-library-v2a1.md` / `v2a2.md` / `v2b.md` / `v2c1.md`
（**同批前作，只读参考，不得抄写**）

**窗口自建，用自己的临时目录**：
`C:\Users\90877\AppData\Local\Temp\opencode\w2c2\*.txt`（带行号前缀 `行号: 正文`）

可写（**仅这两个**）：
- `ai-system/tasks/visual-library-v2c2.md`（素材库 + 采样栅格）
- `ai-system/tasks/visual-v2c2-result.md`（Caveman Review Packet）

> **并行提示**：可能有其他子包 Worker 同时运行。**只动你自己的文件与 `w2c2/` 目录**，
> 不要碰 `w2a/` `w2b/` `w2c1/` 或其他 `visual-library-v2*.md`。

DO NOT
见 `visual-v2-schema.md` §六。额外强调：

- **禁止用脚本生成对象条目**（脚本只可用于定位与自查）
- **禁止一次读完所有窗口再写**——必须边读边写
- **禁止自行扩大窗口数或行数**
- **禁止把抽象规则包装成"视觉对象"**（本阶段最容易犯，见 §二）

DECISION AUTHORITY
可自行决定：窗口内的具体取点、对象颗粒度与命名、关键词措辞、★ 评级（附理由）。
**不可自行决定**：视觉方向、美术风格、任何"应该做成什么样"的结论。

ESCALATE WHEN
- 12 窗读完仍达不到 ≥20 对象（**如实上报，不要凑数**）
- 某窗口全是规则叙述无视觉描写（**记 0 并说明**）
- 原文规模/结构与公共约定给出的数字对不上

DELIVERABLE
1. `ai-system/tasks/visual-library-v2c2.md`：≥20 对象 + 采样栅格（12 窗，允许 0）
2. `ai-system/tasks/visual-v2c2-result.md`：Caveman Review Packet（追加 FACT/ANALYSIS/UNCHECKED）

**预算将尽的处理**：立刻停止采集，保住已落盘内容，写结果包并标 `PARTIAL`。
**绝不允许**为了写完而编造对象或机械切片。

ACCEPTANCE
- ≥20 对象
- 每个对象带行号 + ≥1 条 ≤60 字短引，可由 L2 用 grep 复核
- **无占位名、无跨对象重复转述、无脚本切片痕迹**
- **无「抽象概念包装成视觉对象」的条目**
- 三层标注齐全；「视觉转述」是连贯语句
- 采样栅格齐全（允许窗口 0 对象）
- 无画风结论；无「适合做成」
- `git status --porcelain` 只显示本 SCOPE 内两个文件

STATUS TARGET
READY_FOR_L2_REVIEW
```

## 执行摘要（Worker 请照此报数）

- 范围：**仅 E 阶段**，12 窗 × 150 行（合计 1,800 行）
- 定位：行号 360001–437061，十二等分取中点±75
- 配额：≥20 对象，每窗 ≤3
- 强制：**边读边写**
- 特别纪律：**抽象规则不采**，宁可记 0
- 临时目录：`w2c2/`
- 自查：占位名 0 ／ 重复转述 0 ／ 短引 100% 命中 ／ 三层齐全 ／ 风格词 0
