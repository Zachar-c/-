# 视觉素材 v2 · 子包 2a2：商家城·南疆（L3，READ ONLY）

> **第一步：先读 `ai-system/tasks/visual-v2-schema.md`（公共约定）。本件只写差异部分。**
> 本包模板来自子包 2a1（青茅山，已通过 L2 复核：27 对象、短引 31/31、零占位名），**参数照搬，不要改**。

```text
WORKFLOW ROLE
L3 Visual Lore Research Worker

UPSTREAM
L1 Product Research（任务书）→ L2 Orchestrator（本包）

DOWNSTREAM
L2 复核 → 2b（中洲）→ 2c（北原西漠东海+后期）→ L2 集成 → L1 裁决

MODE
READ ONLY

PROJECT GOAL
把《蛊真人》做成游戏（《问真》）。PRD 定位「以世界运行逻辑为灵感的策略构筑游戏」，
核心幻想「我是一名真正的蛊师」。L0 已禁止直接继承现有水墨方案，要求重研原著后提方案。

CURRENT PHASE
V2 需求重构阶段，**禁止开发**。本任务不写代码、不改数据、不建资产、不做设计。

TASK PURPOSE
建立《蛊真人》视觉素材数据库。本子包负责**资源社会**这一层：
商家城与南疆商道——当一切可以交易时，世界长什么样。
它是青茅山（烟火聚落）之上的一级，也是后面福地/洞天的地面参照。

TASK

## 一、采集范围

| 阶段 | 定位 | 窗口 | 每窗行数 | 对象配额 |
|---|---|---|---|---|
| **B 商家城 / 南疆阶段** | 行号 45001–135000，十二等分取中点±75 | 12 | 150 | **≥16** |

采集重点（L1 指定）：商队 / **坊市** / 拍卖 / 炼蛊师 / 传承 / 战斗

> **省你时间的已核事实**：`坊市` 在全书 **0 命中**（L2 实测）。**不要去找它，也不要写它。**
> 原著实际的商业载体是：**学堂铺**（`学堂` 441 次集中在 A 段）、**酒楼/酒肆**、
> **商队**（`商队` 411 次，A 段 228）、**拍卖**（`拍卖` 320 次，B 段 83）。请采集这些真实存在的形态。

**窗口已由前次派发落盘**，可直接复用（若不存在再自行生成）：
`C:\Users\90877\AppData\Local\Temp\opencode\w2a\B01.txt … B12.txt`（每窗 300 行，带行号前缀）
→ **你只读每窗的前 150 行即可**。

## 二、对象格式

**严格按 `visual-v2-schema.md` §二**：

```text
对象名称：<原文里的具体名字>
原文位置：行号 <N>
原文摘录【事实】：
    行号 <N>：「<≤60 字短引>」
视觉转述【事实】：<≤150 字，一段连贯的话>
视觉关键词【转译】：<3–8 个>
价值评级【评级】：<★1–5> —— <一句话理由>
```

三条最易犯错的：

- **视觉转述是一段连贯的话**，用你自己的话复述原文对该对象的视觉呈现。原文没写的就不写。
  **禁止把同一句切片填进多栏**，**禁止用脚本生成条目**。
- **对象名必须是原文的具体名字**。❌「商队」这类泛称除非原文如此专指 ❌任何 `·行N` 形式。
- **允许某窗口 0 对象**，如实记 0。**不要为凑配额抓无关句子。**

## 三、执行顺序（**照此做，不要改**）

```text
1. 写输出文件头（标题 + 采样栅格表骨架，12 行待填）
2. for 窗口 B01..B12:
     a. 读该窗前 150 行
     b. 就地判断：有哪些"视觉对象"？（0–3 个）
     c. 立即把对象条目【追加】写入 visual-library-v2a2.md
     d. 回到步骤 2 处理下一窗
3. 回填栅格表的「采到对象数」列
4. 跑自查，写结果包
```

**第 2c 步的「立即追加」是本包的核心**（2a1 已验证有效）。不要在内存里攒清单到最后才写。

SCOPE
只读：`蛊真人-clean.txt`、`lore/wiki/**`（仅导航）、
`ai-system/tasks/visual-archaeology-corpus.md`（上轮只读参考，不得抄写）、
`ai-system/tasks/visual-library-v2a1.md`（**同批前作，只读参考，不得抄写**）、
`C:\Users\90877\AppData\Local\Temp\opencode\w2a\B*.txt`（已落盘窗口）

可写（**仅这两个**）：
- `ai-system/tasks/visual-library-v2a2.md`（素材库 + 采样栅格）
- `ai-system/tasks/visual-v2a2-result.md`（Caveman Review Packet）

DO NOT
见 `visual-v2-schema.md` §六（全部适用）。额外强调：

- **禁止用脚本生成对象条目**（脚本只可用于定位与自查）
- **禁止一次读完所有窗口再写**——必须边读边写（见 §三）
- 禁止用 A 阶段或其他阶段对象凑本包配额

DECISION AUTHORITY
可自行决定：窗口内的具体取点、对象颗粒度与命名、关键词措辞、★ 评级（附理由）。
**不可自行决定**：视觉方向、美术风格、任何"应该做成什么样"的结论。

ESCALATE WHEN
- 12 窗读完仍达不到 ≥16 对象（**如实上报，不要凑数**）
- 某窗口找不到视觉对象（**记 0**）
- 原文规模/结构与公共约定给出的数字对不上

DELIVERABLE
1. `ai-system/tasks/visual-library-v2a2.md`：≥16 对象 + 采样栅格（12 窗，允许 0）
2. `ai-system/tasks/visual-v2a2-result.md`：Caveman Review Packet（追加 FACT/ANALYSIS/UNCHECKED）

**预算将尽的处理**：立刻停止采集，**把已落盘的内容保留**，写结果包并标 `PARTIAL`。
**绝不允许**为了写完而编造对象或机械切片。

ACCEPTANCE
- ≥16 对象
- 每个对象带行号 + ≥1 条 ≤60 字短引，可由 L2 用 grep 复核
- **无占位名、无跨对象重复转述、无脚本切片痕迹**
- 三层标注齐全；「视觉转述」是连贯语句
- 采样栅格齐全（允许窗口 0 对象）
- 无画风结论；无「适合做成」
- `git status --porcelain` 只显示本 SCOPE 内两个文件

STATUS TARGET
READY_FOR_L2_REVIEW
```

## 执行摘要（Worker 请照此报数）

- 范围：**仅 B 阶段**，12 窗 × 150 行
- 配额：≥16 对象，每窗 ≤3
- 强制：**边读边写**（读完一窗立即追加）
- 自查：占位名 0 ／ 重复转述 0 ／ 短引 100% 命中 ／ 三层齐全 ／ 风格词 0
