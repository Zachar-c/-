# 你是《问真》项目的最高级代码/产品审计员。

**我：** 你是《问真》项目的最高级代码/产品审计员。

这不是一次普通代码 Review，也不是让你设计新架构。

你的任务是：

**检查当前仓库的实际实现，是否仍然忠实实现以下产品核心，还是已经在局部代码中悄悄偏移。**

核心定义：

* 蛊 = 力量元件，不是普通技能卡。
* 杀招 = 多只蛊 + 顺序 + 资源 + 状态 + 条件形成的运行结构，不应长期退化为“recipe 解锁一个预制技能”。
* 道 = 不同资源、状态、风险和战斗语法，不是元素颜色。
* 转数 = 可承载力量层级，不是装备稀有度，也不是万能倍率。
* 同转蛊允许明显强弱；优秀低转蛊可比垃圾高转蛊更有价值。
* 低转功能蛊在高转仍可能因机制、成本、杀招组合而保留。
* 真正平衡单位是成熟构筑/路线，而不是每只同转蛊。
* 战斗、掉落、商店、炼蛊、经济、修为必须构成一个连续力量循环。
* 信息必须真实改变战斗行为，但 Counter 不能吞掉全部战斗语法。
* 修为提升应增加可承载力量、资源层级和杀招复杂度，而非只做伤害倍率。
* 当前工程原则为 Integration First：能复用不重写，能 adapter 不复制；禁止第四套 Rank、定价、战斗系统。

重点检查：

1. Rank 的所有定义和消费者；
2. 蛊身份、v1_effect、true_qi_cost、value 等字段；
3. 26 条 kill_moves 是否只是预制技能；
4. refinement_recipes 与原著固定炼方、Lab shortcut 的关系；
5. battle rewards、shops、market_rules、refinement cost、突破成本是否形成经济闭环；
6. v1_battle_resolver 与 battle2 的重复/孤立关系；
7. Web Lab balance.js / mvp_content / mvp_logic 是否重复正式世界规则；
8. check_balance / autoplay 是否在验证正式系统还是只验证 Lab 自己；
9. 是否存在“同一个概念在多个地方都有真源”；
10. 是否存在已经写了但没有消费者的孤立能力。

不要先提出新架构。

先输出当前真实状态。

对每个发现使用：

ALIGNED
DRIFT
DUPLICATE
CONFLICT
LEGACY
ORPHAN
INTENTIONAL_PROJECTION

每项必须包含：

* 文件路径；
* 具体字段/函数；
* 当前作用；
* 为什么符合或偏离产品核心；
* 上游 owner；
* 下游 consumers；
* 最小修正；
* 是否需要 L0 批准。

最后只给：

1. 最严重的 5 个偏移；
2. 最值得接通的 5 个断点；
3. 现在绝对不要重写的 5 个模块；
4. 一个按收益/风险排序的 Integration 顺序。

原则：

**不要因为你能设计出更漂亮的系统，就建议重写。**
只有现有实现无法表达已经冻结的产品需求时，才允许提出新增 capability。

**Xiaomi MiMo:** (已完成)

**我：** 为什还在执行

**我：** 总结一下