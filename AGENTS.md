# AGENTS.md

> 本文件随用户指令实时同步（2026-08-25 用户裁定：约束不得固步自封）。当本文件与用户最新指令冲突时，以用户最新指令为准，并同步本文。

## 项目定位

《蛊路求生》：以《蛊真人》世界规则为素材的单机**卡牌肉鸽**（单局目标 3--5 小时、200--300 有效节点，2026-08-25 裁定；map_generator/pacing 后续按此重标定）。机制权威基线为 [机制先行锁死规格书](docs/superpowers/specs/2026-08-25-mechanics-first-lockdown-design.md)（2026-08-25 生效：先锁死核心玩法循环/资源/流派/地图/战斗/交互/肉鸽规则，再用世界观包裹；内含反例黑名单约束与差距审计，新系统提案必须先加机制槽位再领世界观包装）。实现前先阅读该规格书与 [项目决策浓缩对话](docs/项目决策浓缩对话.md)。旧基线 [南疆冒烟版设计](docs/superpowers/specs/2026-08-21-nanjiang-roguelite-smoke-design.md) 仅作参考，不再是权威基线。

## 工作边界

- `分支：六卷精编版/` 只存原文语料与设定提炼物（`蛊真人-clean.txt`、《人祖传》.txt、`读书笔记/`、`记忆库/04-人祖传-隐喻索引.md`、`记忆库/05-设定集-核心锚点.md`），属只读参考资料，改动需用户明确指令。
- `豆包/`、`旧稿归档_不采用/`、`重写稿/` 及 `分支：六卷精编版/` 内其余编辑产物已按 2026-08-25 收敛裁定从工作区与 git 历史整体移除（已同步 gitee），不得重建。
- `肉鸽设计-原始数据/` 为我方检索清单资料，不擅自改写，除非任务明确要求整理。
- 已定方向：卡牌即蛊虫的战斗、炼蛊合成（定向/组合/盲盒）、黑市与 NPC 交易（元石/寿元/魂魄/以物易物）、恶名系统、网状收敛地图、魂魄一心多用（战斗操作上限与炼蛊投入上限由魂魄派生，两曲线非线性）。流派锁定**五道**（血/气/力/魂/炼，2026-08-25 二次裁定）：魂道=主动超额释蛊、反噬转战力；炼道=战斗内简易炼蛊。哲学基调：代价守恒、无隐性善恶奖惩，善意是昂贵选项而非禁忌。
- 蛊方/配方图鉴为**跨局内容解锁**（2026-08-25 已裁定，显式豁免"不做局外成长"条款）：已解锁蛊方/配方录入全局图鉴，后续对局可见、可炼；豁免范围仅限图鉴解锁，不开放其他局外成长。
- 游戏不是纯战斗。资源、情报、人情、交易、撤离、伪装、设局、恶名与战斗都必须是可用路径。
- 不把 LLM 当规则引擎。随机种子、数值、战斗、物品、地图、NPC 隐藏状态、恶名判定、存档和结局必须本地且可复现；LLM 仅能在受控 JSON Schema 下生成有限交涉文本，并必须有离线模板降级。（2026-08-25 裁定：受控 LLM 文本降级 P2，冒烟切片只用离线模板；Schema 契约与降级规则保留，仅排期后移。）
- 所有关键状态变化必须写入不可变结构化事件日志；结局修行札记只能从事件日志和玩家已知事实归因。
- 死亡必须可预见：任何消耗寿元/魂魄的交易、任何反噬结算，执行前必须预检并给出明确文案；不允许静默致死。

## 实现约定

- 技术栈：Godot 4.7.2、GDScript、JSON 数据表、GUT 测试。源代码标识符、JSON 键、测试名称和提交信息使用 ASCII；玩家可见中文文本可使用 UTF-8。
- 蛊虫、蛊方/炼蛊配方、节点、NPC、敌人、商店、恶名与战斗行为保持数据驱动。原著事实、游戏平衡数值与 LLM 文案分开存放。
- 场景只负责展示状态和提交命令；领域规则必须是可测试的纯 GDScript 逻辑，不能由 UI 直接修改状态。
- 架构基线（2026-08-25 九批输入吸收，二十三批补全）：本局全部运行状态收拢单一 `RunData` 纯数据对象，新一局整体重建，禁止散落全局变量；所有 roll 调用经统一种子化随机模块（`PoolManager` 为唯一入口，过滤/排除/保底/权重集中处理）；战斗状态实现统一生命周期接口（on_apply / on_turn_start / on_turn_end / on_remove）；存档仅两套——大厅永久存档与单个进行中 Run 存档（Run 结束即删），序列化只存 id 与数值、绝不存引擎对象；原型内置开发者调试面板（添加任意蛊/修改资源/跳层/查看池排除列表与保底计数/打印 RunData 快照）；输出以 Windows 桌面 Demo 为先，Web 移植后议。规则冲突裁决链锁定：契约＞元机制＞DDA＞卡牌效果＞敌人 AI 行为。
- 引入 GDQuest Open RPG 时保留 MIT 许可证、上游 URL 和固定提交号；未经审计不要直接耦合或修改 `vendor/godot-open-rpg/`。
- 不提交本地工作树、下载缓存、构建产物、密钥或未完成的第三方克隆。

## 工作流程

1. 开始前阅读相关设计与当前 `git status`，保留用户已有的未提交改动。
2. 按实施计划的单个 Task 工作：先写可失败的测试或验收命令，再实现最小改动。
3. 每个 Task 完成时记录实现范围、验证命令和结果，并用独立清单审查变更、边界和回归风险。
4. 提交保持小而聚焦。不要用重置、强制检出或递归删除清理不理解的文件。
5. 推送前确认工作树、测试结果和远端分支；不要声称未实际验证的功能已经完成。

## 当前任务入口

- 已实现（TDD 落地，全量 318 unit + 6 integration 全绿）：评审 §6 全部裁定系统；覆盖批三件（保底基础动作/UI 同步/三流派框架）；**评审优化批 B1–B3**（随机种子/战利品闭环/体验细节）+ **三流派 5×3 初始蛊卡**（取材 11-血/12-力/13-气 检索数据）。
- **B4/B5/资质/防刷批（2026-08-25）**：NPC 三立场（中立/敌视/极度仇恨，高恶名必触发其一；交涉失败种子化翻转敌视；极度仇恨禁逃）+ **情报→弱点→战斗增伤**（probe 产弱点，`intel_bonus` 经 `_strike` 统一结算）+ 黑市魂丹；**资质获得途径**（`aptitude.json.paths` 洗髓换骨：传承/闭关节点、10 寿元+8 元石、一局一次、真元上限实时刷新）；**M5 防刷**（市场二次访问价格 +25%/次、封顶 +100%，卖家同向压价）；**B5 内容扩充**（新敌人 铁皮山猪/雷冠头狼（`greedy_wanderer` 换精英）、卡池 14→19、月光/月华/迹眼战斗分支补全）。
- **机制锁死 P0 批（当前实施批，规格书 §3.4）**：① 印记被动层（规则改变型被动+联动触发+稀有度分级）② 反噬系统（诅咒层；入口=精英奖励附带/事件代价/黑市高回报/盲盒炸炉；必有解除渠道）③ 卡牌移除/替换双渠道（黑市移除服务价格递增 + 休整节点移除负面）④ 稀有保底计数器。五流派扩展批紧随其后（见下条）；其后 P1：休整强制二选一硬约束、精英奖励代价绑定、突破节点 Boss 专属机制、Build 分水岭实测。
- **P0 进度（2026-08-25）**：Task 0 盘点完成——实现代码在本仓库远端分支 `task1-vendor-open-rpg`（gitee），本地检出于 `.worktrees/game-impl`；四问结论=随机单入口/RunState 单对象/两套存档均达标，状态生命周期需沿 `_register_duration_effect/_expire_effects` 模式新建（反噬批）。**T1 稀有度模型已落地**（@ `fee0cf7`）：gu/cards/relics 全量声明 rarity、目录校验拦截缺失/未知值、loot 表改分层权重池且精英层传说权重锁零；验证 329 unit + 6 integration 全绿。Godot 4.7.2 控制台已装于 `C:\Users\Zachary\DevEnv\tools\`。下一片 T2 保底计数器（插 loot_resolver 盐体系）。
- **五流派扩展批（P0 后紧接，规格书 §3.4 二次裁定）**：⑤ 魂道流派 ⑥ 炼道流派（战斗内简易炼蛊）⑦ 局内材料入资源表 + 气道材料保底 ⑧ 三死线可视化预警 + 精准死因弹窗 ⑨ 五流派初始卡组与专属池数据表。
- **进阶边缘系统约束（2026-08-25 三次输入吸收，规格书 §16.1）**：元机制型印记一局 ≤2；蛊化坠落结局随 P0② 反噬系统预留设计；P1 增补=敌人意图池/动态危险等级（UI 明示硬约束）/池排除权/经济硬上限·通胀·应急支付/中途续玩存档；传承三式只碰概率·叙事·一次性效果；叙事只落大厅/事件/结算三处；局内任务系统与局内成就禁入。
- **局内经济约束（2026-08-25 四批输入，规格书 §16.2）**：货币清零/供给硬上限/体系隔离已锁；新增=关键服务黑市专属+单局次数上限、产出风险挂钩、货币保底补额、反噬未处理随层加重、禁存钱道具与局内贷款、结算永久奖励与剩余元石脱钩。
- **战斗 AI 与状态边界约束（2026-08-25 五批输入，规格书 §16.3）**：敌人意图权重池随敌我状态动态化；反制行为族（净化/反噬反击/召唤/以血换蛊）防单套路控死；敌方高危行为强制冷却；敌我同源诅咒系统；状态三要素+同类叠法全局统一+硬控对 Boss 限回合并短暂控免；伤害双通道（反噬绕盾直扣生命）；封印只禁新开不抹已生效；死亡遗言先于死亡结算；强力蛊三类代价至少绑二；触发类效果单回合硬上限+【蛊躁动】。
- **池管理约束（2026-08-25 六批输入，规格书 §16.4）**：未解锁内容不进局内随机池；保底只抬品质下限不指名具体蛊、禁直给传说、商店消费不计数；奖励来源隔离（普通战/精英/商店/事件各用差异池）；诅咒只可主动选择获取不入普通掉落；移除≠池排除（UI 必须区分）；空池回退/单次奖励去重/隐藏池独立；传说软上限递减；品质权重参数与最小池架构见 §16.4。
- **UI 信息透明约束（2026-08-25 七批输入，规格书 §16.5）**：tooltip 固定格式（品质/效果/联动/代价/诅咒警示，数值写死禁模糊文案）；敌人意图必须含数值与效果文字；生命与护盾分条展示；商店服务显示剩余次数与通胀说明；事件选项后果预览+侧边查自身状态；无读档按钮只有继续上次冒险；保底计数只暗示不裸露；全局 debuff 与契约效果常驻顶部；结算六要素复盘；诅咒蛊强红色视觉警示。
- **页面流转约束（2026-08-25 八批输入，规格书 §16.6）**：大厅四分支（继续 Run 优先/流派选择+契约占位/图鉴/设置）；地图八类节点跳转与统一结算入口；蛊槽满强制取舍弹窗禁静默覆盖；空池回退带小字提示；封印销毁轻提示不阻断；所有结局必走结算页；全程无读档回溯按钮。
- **轮回叙事约束（2026-08-25 十一批输入，规格书 §16.9）**：主角跨局只有碎片手记、无清晰记忆，NPC/残影可模糊感应但主角不得完整回忆；叙事分大厅手记库/局内事件短句/结算结局文本三层；解锁条件=行为路线与结局（非次数计数）；主线不强制锁单局流程；全部剧情可跳过；叙事只产手记线索不产战力；轮回感应类增益必须开局契约且默认关闭；残影只复用行为组合、属性按本局缩放。
- **非死亡结局约束（2026-08-25 十二批输入，规格书 §16.10）**：非死亡结局必须玩家主动二次确认，数值阈值只解锁选项、不强制结束 Run；分支需本局行为/线索解锁；收益排序=死亡/失败求生＜中途遁走＜蛊化坠落＜险成＜成功＜真结局；全部结局统一同一结算模块（`ending_type` 字段驱动，结算顺序固定），Run 存档结算时删除；结局只产手记/图鉴/开局契约，绝不产跨局战力；献祭封印类变体列 P2 候选。
- **动态难度自适应约束（2026-08-25 十三批输入，规格书 R14.5–R14.6）**：DDA 分进度驱动（必做）与本局状态驱动（谨慎）；只读本局 Run 内部数据、禁读跨局历史；手段优先级=敌人池＞地图＞奖励供给＞经济，数值乘算仅兜底且限小区间；触发即生成可见元机制标记（险象/衰运）顶部常驻并与契约 UI 分区；同类互斥至多一条、有阈值上限并保留补救路径；只调概率绝不直赠战力；大厅提供关闭开关；Boss 局部自适应战斗结束即清并 UI 提示；结算复盘展示触发记录；系统侧标记不计入玩家元机制上限。
- **轮回传承约束（2026-08-25 十四批输入，规格书 §16.12）**：传承三类分界=叙事（优先做）/概率（严格封装为开局契约且须附对等负面代价）/战力（严禁）；大厅存档仅存手记解锁、契约解锁与总统计，绝不持久化上局 Build 或临时权重，残影生成用后即弃；轮回积累不得磨平契约/诅咒/反噬的代价体系；轮回统计纯展示零加成。
- **契约系统约束（2026-08-25 十五批输入，规格书 §15/§16.13）**：契约=开局全局规则修改器，收益与对等代价成对存在、禁纯正向（自苦型除外）；回报只走大厅素材倍率、不在本局给实物；规则修改优先数值兜底；同时启用软上限 5–6 条且互斥 ID 入 `contracts.json`；普通手段不可撤销、仅极罕见高代价事件可弃单条；解锁绑结局/手记不刷次数；注入 `RunData.meta_rules` 统一执行并与本局异变 UI 分区（契约蓝系/异变黄红系）；预设组合与纯负面契约列灰区约束。
- **背包与仓库边界（2026-08-25 十七批输入，规格书 §16.15）**：无跨局仓库，`RunData.inventory` Run 结束清空不入大厅档；蛊槽硬上限、扩容仅经事件/奖励少量获得且设全局硬顶并附代价；契约与元机制非背包物品不可丢弃；部分诅咒蛊标记 `can_direct_drop:false` 强行丢弃触发负面事件；手记解锁即入大厅不进背包；消耗品限囤积；大厅图鉴只读不可取出到新局；封印蛊道具=失效仍占槽（灰区）。
- **掉落商店材料闭环约束（2026-08-25 十八批输入，规格书 §16.16）**：四类物资（蛊/消耗品/材料/元石）本局专属结局清空已锁；新增=构筑饱和反倾斜（对立流派权重适度上调不锁死）、拆解化材（被拆解蛊直接消失换材料，仅限炼蛊台/事件节点）、事件奖励可产元机制印记但普通战斗/宝箱掉落不可、商店代价交易栏（躁动/生命上限/占槽换强力物品）与净化躁动服务、`drop_table/shop/material` 配置表分离；保底计数器可被契约修改并纳入结算复盘。
- **等级轨裁定（2026-08-25 十九批输入，规格书 §16.17）**：不设平行经验等级，单局成长轴唯一=修为/转数（五转封顶）；指南约束按映射吸收——节点一次性禁刷怪（R3.6）、突破/成长奖励多选一功能化（R8.3）、属性增幅克制、大厅无等级只有解锁、契约可改修为获取速率（contracts 目录候选）、结算展示最高转数。
- **炼蛊合成补强（2026-08-25 二十批输入，规格书 §16.18）**：定向配方禁零代价毕业（高阶必绑显著代价并受传说软上限约束）；诅咒蛊参与合成负面继承放大、不得洗白；合成前预览全部风险+二次确认+空位校验；连续失败补偿计数永不到 100% 且 Run 内清零；配方方向=同流融合/冲突交融变异/蛊+材料产诅咒蛊；`synthesis.json` 配置分离，契约对成功率的修改入 `meta_rules`。
- **牌组与杀招裁定（2026-08-25 二十一批输入，规格书 §16.19）**：牌组结构锁定单层蛊囊（卡组上限=持有上限，不采用双层持有+出战编成）；战斗内只产临时卡，真增删牌组仅在地图节点生效；爆牌进弃牌堆、手牌有上限；流派组件可替代禁缺一不可；杀招采**条件触发式**（无充能槽）：组合/蛊蚀层数/绝境血量三类解锁，释放必附沉重代价（躁动暴涨/真元减损/扣生命上限至少其一），不清除系统负面、不拯救崩盘 Build、动画可跳过；Boss 拥有镜像杀招供玩家延缓博弈。
- **图鉴系统约束（2026-08-25 二十二批输入，规格书 §16.20）**：图鉴=大厅永久纯查阅系统、绝不可提取到对局；遭遇即解锁（不需获取击杀）、即时写入、死亡不回滚；临时变异蛊不入图鉴只录原型；收集奖励仅叙事插画、零战力零掉落加成；线索提示模糊引导不给精确攻略；模拟预览仅 UI 演示不入局；`hall.codex` 存档 + `codex.json` 配置落地。
- **开发者工具与版本管控（2026-08-25 二十四批输入，规格书 §16.22）**：调试控制台仅存于开发构建（Release 编译裁剪，禁止仅 UI 隐藏）；调试指令只写 `RunData`、绝不修改大厅存档与解锁状态、不绕过正式背包/容量校验；全部调试操作写入运行日志可追溯。调参一律落地 JSON 配置文件为唯一真值来源，经 Schema 校验拦截错误配置；试验配置与正式池隔离；调参工具不碰玩家存档。分支策略 main（稳定可玩）/prototype（试验）；JSON 配置纳入版本管理、提交注明修改意图；RunData 存档不做跨版本兼容，hall 存档尽量向前兼容、版本差距过大提示重置；试验功能用配置开关，不留注释废码；平衡性评估走正常随机流程，禁止用调试 Build 下结论；每次合并执行最小闭环回归清单（开局契约→获取蛊→商店→合成→战斗杀招→任意结局结算→大厅清零验证）。
- 剩余候选：怪物/卡池继续扩充、NPC 个人库存（N-候选）、更多资质途径（数据口已开）。