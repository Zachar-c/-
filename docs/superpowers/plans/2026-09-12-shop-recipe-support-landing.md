# 商店分栏 / 剑道跨道配方 / 辅助蛊实义 需求落位（2026-09-12）

> 用户四问落位（Q6 空缺，用户未给内容）。每问给出**现状核实 → 拆解 → 施工批次**。
> 纪律沿用任务书：先测试后改数据；每批单独提交；全绿才算完成。

## Q4 · 商店分栏展示 + 卖出

**现状核实**：
- 货架=`ShopRulesScript.shop_stock(state, catalog)`（seed 洗牌 + 本流派保底，`a7c8ae0`）；
  商店屏 `shop_screen_view.gd` 单列表渲染 offers。
- **卖出命令已存在**：`sell_gu`（`refine_command_rules.gd:32`）、`sell_material`（`:767`），
  预览钩子也有（`action_preview_service.gd:360/:1051`）——但**商店屏未挂卖出入口**（用户感知"不支持"即此）。

**拆解**：
| # | 任务 | 类型 |
|---|---|---|
| Q4-1 | 商店快照/屏分三栏：**蛊虫栏 / 材料栏 / 服务信息栏**（offer 按 gu_id/material/服务分类渲染） | 数据+UI |
| Q4-2 | **固定栏位**：常用低端材料（月兰花瓣/兽血/兽骨类）与常见蛊（石皮蛊/兽皮蛊/粉啄蛊类）不参与洗牌，常驻货架 | 数据+规则 |
| Q4-3 | **稀有栏位刷新**：不常见蛊按固定稀有栏位刷新展示（刷新盐=seed+到店次数） | 规则 |
| Q4-4 | 商店屏挂**卖出入口**（蛊虫+材料），复用既有 `sell_gu`/`sell_material` 命令与预检预览 | UI+命令接线 |

**风险**：Q4-2 固定栏与洗牌保底逻辑的叠加面；固定清单须以 material_ids / gu 池真实 id 为准（用户口语"粉啄蛊"等需比对 names.json 后定名）。

## Q5 · 剑道配方新增（跨道合炼）

**现状核实**：`refinement_recipes` 386 条；剑道配方已随批次 1/T15 落地（`a7c8ae0` 补成长链）。

**拆解**：
| # | 任务 | 类型 |
|---|---|---|
| Q5-1 | 按杀招线/机制语义新增剑道炼制配方：**原料明确含非剑道蛊/材料**（如炎道剑意共鸣、血道淬锋）——呼应原文"道路万千"与改良生态 | 纯数据 |
| Q5-2 | 配方须声明 `output_rank` + 通过既有校验（rank gating/前置持有）；产出卡文案标"跨道合炼" | 纯数据 |

**边界**：不做流派克制网；跨道只是**原料来源**语义，不是跨道加成倍率。

## Q7 · 辅助蛊实义化（recon/logistics 10 只 + 全流派辅助蛊）

**现状核实**：
- T15 刻痕通道已实现（`fc4c179`）；T8 `sword_intent` 契约桩已落（引擎未接线）。
- 剑道 recon5/logistics5 当前走兜底 status（marked/bound），batch-1 D3 裁定"暂不动"——**被本问推翻，需落地**。
- 引擎事实仍有效：`buff` 只喂 basic_attack；`status` 不参与伤害 → 实义化优先用**已有通道**：
  `support_school/support_bonus`（下一次同流派蛊 +N）、`sword_intent` 接线（叠层速度）、
  `durability CONSUME_ON_USE`/`life_cost`（代价型）、T10 剑道底蕴（mark_sword，长线）。

**拆解**（每只辅助蛊按五类语义各归其位）：
| 语义 | 通道 | 引擎改动 |
|---|---|---|
| 增加剑意叠层速度 | recon 蛊使用 → `add_sword_intent` 接线 | 小改 resolver/school_rules 调用点 |
| 增强下一次剑道蛊伤害 | `support_bonus`（已有） | 零 |
| 同轮双剑蛊 1+1>2 | 起势链（已有，批次1 实证）+ 更多蛊入起势档 | 零 |
| 代价型（损而後用） | `life_cost`/`CONSUME_ON_USE`（已有） | 零 |
| 增加剑道底蕴 | T10 `mark_sword`（长线道痕，1 道≈0.1% 量级） | 接 T10 |

**配套**：卡牌文字与实际结算一致（snapshot 文案逐只核对）——红线"UI 缺陷要断言界面"。

## Q8 · 位移转防御 ✅（本批已施工）

- shift 结算 = `shield += amount`（同比转化），position 不再推进；
  距离减伤/敌人追击因 distance 恒 0 自动失效（死路径保留，清理批次删）。
- 卡牌文字改「退守：护盾 +N」；推翻 `specs/2026-09-12-shift-distance-spec.md` 距离模型（保留文件+更正横幅）。
- 测试：剑道契约 27/27；effect_guards/basic_actions/battle_screen 26/26。

## 施工顺序

```
Q8（已做）→ Q7（辅助蛊实义，依赖 T8/T15/T10 通道）→ Q5（配方纯数据）
  → Q4-1/4-2/4-3（货架分栏+固定+刷新）→ Q4-4（卖出入口）
```
