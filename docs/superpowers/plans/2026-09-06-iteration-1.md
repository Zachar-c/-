# 第一批迭代 · 派发排布（Iteration 1）

> 日期：2026-09-06
> 依据：`docs/superpowers/plans/2026-09-05-architecture-refactor-master-plan.md`（工单定义）+ `AGENTS.md`（红线）
> 状态基线：B 系（A-1/B1/B2/B3，39 commits）已全量绿并备份 gitee（远端 == `ec10917`，分支 `测试，用完就丢`）；并行 visual/audio 会话未合拢（工作树 ~195 项未提交，其中 `gu_names.json`/`schools.json` 等 data 面被占用）
> 目标：D 配方体系首切 + B 系遗留收敛 + 主线整合，为「配方驱动合炼」核心支柱铺到可派发状态

---

## 门槛 B1-0：并行合拢（外部，非本批执行项）

- 并行 visual/audio 会话将其工作树提交合拢（data/presentation/audio 面稳定、`gu_names.json`/`schools.json` 解占用）。
- 判定：`git status` 连续两采样稳定 且 data/presentation 无未提交改动；`git log` 出现并行新合入。
- 影响：I-3/I-4 依赖该门槛；I-1/I-2 不依赖（文件面 clean，见各批）。

## 批次 I-1：主线整合（B 系 + 并行成果入 master）— ✅ 完成 2026-09-06

- 内容：将 `测试，用完就丢`（A-1/B1/B2/B3 + I-2 + 并行视觉/audio 成果，50 commits）并入 `master`。
- 前置：用户裁定合并时机（建议并行合拢后一次性整合，避免 master 与并行内容双轨漂移）；用户于 2026-09-06 宣布并行合拢（60s 写入采样零新增佐证）。
- 方法（只读先验，符合 git 纪律，禁 reset --hard）：并行成果先按域分组收拢提交（audio `c12190b` / wenzhen 美术 `bd1ed75` / presentation 视觉 `a85f550` / 视觉配套+AU1 `dfdfa23`），再零分叉快进（master 是分支直接祖先）。
- 收拢中修复（并行成果与既有契约冲突）：
  - `test_audio_director` 5 红清零：`audio_director.gd` BGM_PATHS 迁 .mp3/.ogg（hall/battle→mp3、map/ending→ogg），循环强制改类型感知 `_force_loop`，断言 WAV-only 通用化（AU1 收尾）。
  - `test_wenzhen_master_theme` pressed 契约适配 shadcn 实心按钮（背景深浅表达按下态，fg 必变断言改为 bg 未变时才强制）。
  - `test_wenzhen_a1_drag_mount` 退役：Battle 主路由已切官方 `battle_screen.tscn`，`wenzhen_battle_master.tscn` 生产零引用；官方屏出牌契约由 `test_wenzhen_battle_screen.gd` 11/11 + smoke 覆盖。ui_masters/ 文件清理归入待办②③。
- 验收：master 快进至 `8c45a48`；unit 1093/1093 + integration 31/31 + smoke 31 OK exit 0；`push origin master`（wincred，39s，`1b57e5d..8c45a48`）；分支同步 `8c45a48`。
- 顺带：`AGENTS.md`「当前待办」已同步为合拢后真实状态（`b0233fc`）——清除已完成项、刷新基线数字。
- ⚠️ 意外：收拢期间并行会话第三次整目录物理删除（tests/unit 175 + tests/integration 12），已按 B2 预案 `checkout-index` 恢复无损；SIGTERM 残留 index.lock 已清。

## 批次 I-2：B 系遗留收敛（自主，不依赖并行）

**I-2a 预览子系统收敛（B1/B2 记录遗留）**
- 现状：`preview_battle_actions`（`scripts/domain/action_preview_service.gd:99-123`）两分支查已退役 `catalog.card_by_id` 恒空 → **V1 战斗蛊槽预览只出拳脚/闪避/撤离，无蛊行动卡**。
- 内容：V1 蛊槽预览改由 `gu_by_id` + `slot`（v1_effect/rank/名称/稀有度）投影蛊行动卡；旧 `battle.hand` 信封分支按残余消费判定（删或冻结）。
- 前置：`test_action_preview_service.gd` 红先行；先确认 `run_snapshot_builder`/`command_spec_registry` 对 preview 的消费链。
- 验收：定向 GUT 绿 + 契约测试不回归 + `rg card_by_id scripts/` 零命中。
- ✅ 完成（2026-09-06）：侦察证明蛊行动**不经** battle.action_card——`run_command_builder.gd:51` 手牌行 id `gu.<instance_id>` 直接构造 `use_gu`；UI 手牌由 run_snapshot `_v1_hand`（gu_by_id）独立投影。preview 中查已退役卡蓝图索引的 hand/蛊槽投影为死代码，已移除（连同 `_append_battle_hand_card`）；preview 保留信封基本动作。验证：`rg card_by_id scripts/` 零命中；test_b5 4/4、test_preview_retreat_gate 3/3、test_action_preview_service 11/11、test_command_contract 11/11 绿。

**I-2b D3 杀招数据地基** → **⚠ 规格矛盾，待裁定**
- 内容（原计划）：`gu.json`/`refinement_recipes` 的 synergy 键 Schema 预留（类型白名单 + 注释，不实现玩法、不快照透出）；`docs` 记录月光蛊+小光蛊=2x 目标语义。
- 验收（原计划）：Schema 校验绿；`rg "kill_move" scripts/presentation/` 零命中（确认无越界实现）。
- **矛盾**：V1 已实现杀招系统（`v1_battle_resolver` `kill_moves` 组装 + `play_kill_move` 命令 + `data/v1_battle.json` + 战斗屏 KillHost + run_command_builder 杀招命令）——「杀招本版推迟」假设已被推翻，`rg kill_move scripts/presentation/` 无法零命中（战斗屏/run_snapshot 正当使用）。D3 验收线过期，需按核心支柱（玩家组装杀招）修订后方可执行。
- ❌ **关闭（2026-09-06 用户裁定）**：V1 kill_moves 即杀招数据地基；月光蛊+小光蛊=2x 组合加成并入正式杀招系统开发（2026-09-01 规格）一并设计。主计划 D3 已回写关闭。

**I-2c 配方账实对账（只读审计）**
- 现状：AGENTS 审计记录「配方 393 vs 记录 386 待对账」。
- 内容：对 `refinement_recipes.json` 实际条数、kind 分布与主计划/AGENTS 记录对账，输出差异说明（是记录过时还是数据漂移）。
- 验收：对账报告文档。
- ✅ 结论（2026-09-06）：**无 +7 漂移**——`85150f7` 与 HEAD 均为 `recipes`=386（377 advance + 8 fixed + 1 free_mix）+ `caravan_offers`=4，与计划记录 386 一致。审计「393」为该时点误读（计数口径含非配方条目或工作树中间态）。「疑 D1 剩余已部分执行」**部分属实**：`8f9cca9`（feat d1，并行会话 2026-09-06 11:24）已给 9 条策展配方（8 fixed + 1 free_mix）落 `source` 原文锚点 + v2 跨流派 inputs。D1-剩余实际剩余 = 更多配方 source 补全（按摘录文档其余条目）+ R6-R8 裁定 + synergy 预留（见 I-2b）。

## 批次 I-3：D1-剩余（配方源首切）— ✅ 收口 2026-09-06（核对完成 + 扩充移交专项）

- 状态：R1-R5 落表全部核验到位；配方完整性审计完成（发现高转链缺口）；用户裁定——**开 D1b 专项补跨流派合炼矩阵**（主计划 Phase D 新增工单），缺目录蛊归审计清册二阶段（§6.1 登记 8 项）。本批无代码/数据改动（扩充全部移交外部输入）。

- 前置：I-1（master 基线）+ 门槛 B1-0（gu_names 解占用）。
- 侦察后现状（2026-09-06）：D1 大部分已落地——Schema v2 校验已在 `content_catalog.gd:629-671`（advance 一对一、output_rank/input_min_rank 1..5、**fixed/free_mix source 必填**、v2 inputs school/rank/count）；`8f9cca9`（并行）已给 9 条策展配方（8 fixed + 1 free_mix）落 source 锚点 + v2 跨流派输入；`moon_ray_gu` 已在目录且 `gu_names.json` 已命名「月痕蛊」（摘录 R2「名缺」已解决）。
- 实际剩余：
  1. **跨流派/高转方扩充**（R4/R4b/R5 骨架已有，按摘录补足 2→3 转光×气/光×血等跨流派进阶链）；
  2. **R6-R8 输出蛊策展裁定**（见下决策包）。
- 验收：Schema 校验绿；每条配方领域校验通过；新增方全部带 source/v2 结构。

### I-3 裁定决策包：R6-R8 缺目录蛊（2026-09-06 侦察）

| 原文蛊 | 输入（原文） | 目录现状 | 建议 |
|---|---|---|---|
| R6 月霓裳（2转） | 月光蛊+玉皮蛊（clean 15726-15784，终点五转宝月光王蛊） | 月光蛊(moonlight_gu)✓、玉皮蛊(jade_skin_gu, earth 1转)✓；输出蛊无对应 | 归内容策展线（审计清册二阶段）加蛊，或弃（不收入首批路线） |
| R7 月痕蛊 | 月光蛊+痕石蛊（clean 17152） | **moon_ray_gu ✓ 且已命名月痕蛊**（light 2转）——已代表，无需再加 | ✅ 视为已解决（确认 moon_ray=月痕蛊 语义） |
| R8 月旋蛊 | 月光蛊+旋风蛊（clean 17154） | 旋风蛊 catalog 无对应 id（light 无 wind 分支蛊） | 归内容策展线，或弃 |

> 注：策展线已由 AGENTS 明确为「保留蛊效果与原文逐只核验（审计清册第二阶段）」，新增蛊（含月霓裳/月旋及痕石/旋风等输入蛊）应随该线提供策展条目后落库，再补配方；本批不越权新增目录条目。R7 视为已解决。

### I-3 侦察补充（2026-09-06，配方完整性审计）

1. **R1-R5 落表核对**：R1 moon_glow_fixed（v2+source ✅）/R2 moon_ray_forged（v1 精确蛊 id+source ✅，保留精确语义）/R3 white_jade_advance+basic（v2+source ✅）/R4 moon_shadow_locked（v2+source ✅）/R5 blood_moon_forged（v2+source ✅，out=blood_atk_3_11_gu「血月蛊」）。R1-R5 全部就位。
2. **R4b 幻月蛊实际缺失**：`phantom_moon_locked` **不存在于 386 方**（摘录文档误记"现成"）；且 `phantom_moon_gu`/`mist_step_gu`/`shadow_veil_gu` 三蛊全不在目录 → 归策展线（同 R6/R8）。另 `moon_shadow_locked` 依赖的雾步蛊实体缺失（v2 以 {qi,1,1} 泛指），雾步蛊实际转数（原文）待策展裁定。
3. **高转合成链缺口（真实玩法缺口）**：产出 rank3+ 蛊的配方**全库仅 2 条**（blood_moon_forged/moon_shadow_locked，均 light2+另道1）；180 只 rank3 gen 蛊**零 rank2 同名前身**（`blood_atk_2_xx`≠`blood_atk_3_yy` 无同族，机械补 advance 不可行）；rank4/5 共 244 只更无产出方；loot 仅 elite epic 掉 1 只 4 转蛊。→ 玩家合炼到 2 转（月芒/月痕/白玉）后**无路到 3+ 转**，与核心支柱「海量合成配方」断裂。补齐 = 为 422 只高转蛊设计跨流派合炼矩阵，属内容策展设计（非 D1 数据机械化）。
4. **v2 rank 语义确认**：`content_catalog.gd:629-671` 校验只验 school 存在/rank∈1..5/count≥1，不验输入/输出转数关系；rank 语义（"最低可用" vs "精确"）由 refinement 运行期 gate 决定 → 现有 2 条 3 转配方的次输入 rank1 是合法的"任意 1 转该道蛊"，策展精度问题非错误。

### I-3 实际剩余（全部依赖外部输入）

| 项 | 内容 | 依赖 |
|---|---|---|
| 缺蛊策展 | R4b 幻月/月霓裳(R6)/月旋(R8) 输出 + 雾步/影幕/痕石/旋风 输入（共 7 只缺蛊）+ 命名/转数裁定 | 审计清册二阶段或用户授权本批策展 |
| 高转合炼矩阵 | 422 只 3-5 转蛊的跨流派合炼链设计（打通核心支柱） | 内容设计裁定（可作为独立 Phase 派发） |
| v2 rank 语义对齐 | 雾步实际转数、血月输入转数等策展精度 | 随策展逐条裁定 |

## 批次 I-4：切片内容首切（S 系最小闭环）

- 前置：I-3 + A-2/新 UI 栈合拢（视觉会话）。
- S4 元素协同（优先，域内为主）：`v1_effect` 支援类（小光蛊「本回合月光系伤害+2」）结算 + 预览 + 单测。
- S1 光道开局：`schools.json` 每流派 4 只 1 转初始蛊、光道加入开局选择（依赖 schools/gu 稳定 + 新 UI 开局流程）。
- 验收（S4）：单测覆盖结算与预览；真实窗口小光蛊→月光蛊连用伤害 +2（需真实窗口验收）。

## 待用户裁定清单

| 项 | 选项 | 影响 |
|---|---|---|
| C1 主线合并时机 | A. 并行合拢后一次性整合（推荐） / B. 现在先合（并行内容后续再合） | I-1 |
| C2 I-4 是否纳入第一批 | A. 纳入（S4 先行） / B. 留到第二批（本批止于 I-3） | 首批范围 |
| C3 R6-R8 处置方向 | 逐蛊：加（策展，需给名字/效果/rank）或弃（映射现有蛊） | I-3 步骤 2 收尾 |

## 验收汇总（第一批结束时）

- master 全量 `test.ps1 -Suite all` + `check.ps1` 绿；
- `rg card_by_id scripts/` 零命中；`rg "kill_move" scripts/presentation/` 零命中；
- 配方溯源条数提升（对账表可复核）；
- AGENTS.md 当前待办已同步；本批派发工单在主计划逐项回写 ✅。
