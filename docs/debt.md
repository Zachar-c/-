# 债务清单

> 格式：`路径 | 类型 | 现状 | 处理 | 权威/理由`。类型只使用 `KEEP`、`MOVE`、`MERGE`、`ARCHIVE`、`EXCLUDE`、`REVIEW`。本文件只记录债务，不做管理系统。

| 路径 | 类型 | 现状 | 处理 | 权威/理由 |
|---|---|---|---|---|
| **P0** `蛊真人.txt`、`蛊真人-clean.txt`（仓库根，23,520,121 / 23,172,557 字节） | EXCLUDE | **未闭合（2026-09-19 实测）**：两份完整原文被 Git 跟踪（分别自 `02a98fb`、`da282b0` 起），并同时存在于 `main` 与 `origin/main`（`git cat-file -e <ref>:<路径>` 四次全部命中）。远程 `https://gitee.com/chen-dong-s/gu-zhenren` 经匿名 `info/refs?service=git-upload-pack` 实测返回 HTTP 200 并广播 `9ba52c7`，即公开可读、原文可下载。本行推翻下表第 12、14、18、26 行的“已闭合／全树无跟踪记录”结论；漏检原因：审计只核对了 `game/`、`gu-zhenren-editor/` 子目录副本与 `source/` 目录，未覆盖仓库根同名文件 | 当前树止血已执行：`git rm --cached` 两份根原文（本地磁盘副本保留，供 Wiki 蒸馏继续按行号回读），根 `.gitignore` 三条原文规则改为未锚定（任意层级一律不入库）。**历史清理未执行**：`da282b0` 已进入 `main`、`origin/main`、`codex/phase2-first-godot-task`、`codex/phase2-second-godot-task`、`origin/workflow-refactor`，重写需用户单独裁定；在边界恢复并经复核前冻结 push 与新提交合并 | 根 `AGENTS.md` 禁止事项；`CONSTRAINTS-V2` §2 法律边界；2026-09-19 用户裁定「P0 先止血、不 `filter-repo`、不 force push、不删远程分支」 |
| `gu-zu/`（旧根目录入口） | MOVE | Task 5 已完成：过滤后 subtree 导入 `lore/research/`（`072aa6e`，206 文件，blob 级 206/206 一致）；旧快照 206 个受版本文件已 `git rm`，仅删真正空目录；残留空壳 `gu-zu/` 下只有被忽略的本地恢复材料 | 保留残留 `gu-zu/.git-nested-backup/`、`.worktrees/` 作本地恢复（根规则已忽略，不进入 Git 树）；Task 8 最终验收时复核 | 计划 Target Mapping；Task 5 Verification Log |
| `wenzhen-lore/`（旧根目录入口） | MOVE | Task 5 已完成：35 文件以 `git mv` 扁平迁移到 `lore/wiki/`，只做机械路径更新（旧 blob + 替换程序重建 35/35 字节一致）；旧目录已确认无被忽略文件后 `rmdir` 移除 | 关闭 | 计划 Target Mapping；设计 Wiki 冻结边界 |
| `gu-zhenren-editor/`（旧 Gitee 游戏工程快照） | MOVE | Task 7 已完成：过滤后 subtree 导入 `game/`（`ebb7f81`，744 提交、1904 文件）+ 本地带过提交 `db34873`（`game/wenzhen-web/` 10 文件，逐 blob 一致）；旧快照 1758 个受版本文件已 `git rm`（`git ls-files -- gu-zhenren-editor` 为 0）；`.git-nested-backup/`、`.worktrees/`、`.godot/`、两份本地原文（151,392 / 23,172,557 字节）、10 个 `tmp*/` 保留在磁盘。Task 8：残留约 4500 个未跟踪文件（全在本地工具目录内），根 `.gitignore` 新增 `/gu-zhenren-editor/` 整目录忽略，`git status` 已干净 | 关闭受版本内容。残留本地恢复材料保留在磁盘（不进入 Git 树）；旧快照自带嵌套 `.gitignore` 已随 `git rm` 离开 Git 树 | 计划 Target Mapping；`MIGRATION.md` Task 7/Task 8 小节 |
| GitHub `gu-zhenren-editor`（与上行同名但不同来源） | MOVE | Task 6 已完成：过滤后 subtree 导入 `editorial/`（`0cbce85`，183 文件、83 提交；`editorial-final` 与 `mirrors/editorial.git` 共有路径 blob 183/183 一致，路径差集只有 `蛊真人.txt`） | 关闭。本行来源是编辑部资料库，上行是 Gitee 游戏快照，两者不可互相覆盖 | 计划 Target Mapping；`MIGRATION.md` Task 6 小节 |
| `fortune/app/`、`fortune/server/`、`ai-system/` | MOVE | Task 6 已完成：三个过滤镜像分别 subtree 导入 `fortune/app`（`6603115`，36 文件、21 提交）、`fortune/server`（`0240567`，3 文件、1 提交）、`ai-system`（`479145d`，1 文件、1 提交）；路径清单与各自镜像 tip 逐行一致；未复制 `fortune-app/fortune-app/` 外层包装目录 | 关闭 | 计划 Target Mapping；`MIGRATION.md` Task 6 小节 |
| `editorial/蛊真人.txt`（仓库根完整原文底稿，23.5 MB） | EXCLUDE | Task 6 首次原样导入时随本地提交 `3afe2dd` 进入本地历史（未推送）；用户裁定后以 `filter-branch` 重建 `editorial-final` 并重新导入，`0cbce85` 及其后历史均不含该路径 | 已闭合于**该 `editorial/` 路径**：`0cbce85` 及其后历史不含该路径。**但本行的“当前分支与后续提交不含完整原文”结论已被 P0 行推翻**——仓库根另有 `蛊真人.txt`／`蛊真人-clean.txt` 两份完整原文进入并推送至 `origin/main`，全仓边界仍未闭合。`3afe2dd` 只留本地 reflog；是否需要清理本地悬空对象由用户决定，worker 不自行处理 | 用户 2026-09-18 裁定；根 `AGENTS.md` 禁止事项；2026-09-19 P0 复测 |
| `editorial/volumes/**/*.edited.txt`、`editorial/volumes/*/*.epub`、`editorial/working/archive-2026-08/read-*.txt`、`round3-*.txt`、`working/*.cp936.txt` | KEEP | 用户 2026-09-18 裁定：除《蛊真人》《人祖传》原始成书外，其余成书均为本项目精编产出，不应排除；`working/` 内是同一批资料的工作副本 | 保留在 `editorial/`，不再按原文排除。**遗留边界复核点**：`working/read-*`、`round3-*`、`cp936` 是带盗版站水印的原始章段切片，合起来接近完整原文，与根 `AGENTS.md`「禁止提交完整原始小说」存在张力，Task 8 终验时提请用户复核 | 用户 2026-09-18 裁定；根 `AGENTS.md` 禁止事项 |
| Task 4 审计模式缺口（`蛊真人.txt` 精确名、`*.epub`、大 blob、`core.quotePath` 转义） | REVIEW | 已闭合（Task 8 实测）：`rev-list --objects --all` 全历史扫描——`clean\|人祖传` 命中仅工具脚本（`clean_full_source.py`、`source-clean-candidates.tsv`）与派生索引（两处 `04-人祖传-隐喻索引.md`），无完整原文；`.epub` 命中仅 `editorial/` 精编产出与构建脚本（用户裁定 KEEP），`game` 历史零 `.epub`；`verify-pack` 最大 blob 151,477 字节（包总量 549,587 字节，23 MB 原文不可能藏匿）；全部检查均加 `-c core.quotePath=false` | 关闭；补检证据见 `MIGRATION.md` Task 8 小节 | Task 6 实测；`MIGRATION.md` Task 4 与 Task 6 小节 |
| Task 4/Task 8 “全树无完整原文”审计结论 | EXCLUDE | 已被 P0 行推翻（2026-09-19 复测）：其“无完整原文”与“最大 blob 151,477 字节”在当前合并后的仓库不成立——HEAD 树内实测存在 23,172,557 与 23,520,121 字节两个原文 blob（仓库根 `蛊真人-clean.txt`、`蛊真人.txt`）。原扫描按 `clean\|人祖传` 关键词命中，未覆盖不含 `clean` 的根目录 `蛊真人.txt`，`verify-pack` 指标亦非当前仓库状态 | 重开为未闭合；任何“全树无原文”结论在按 `-c core.quotePath=false` 的 `git ls-tree -r -l HEAD` + `rev-list --objects --all` 双重复测通过前不得再写为已闭合 | 2026-09-19 P0 实测；根 `AGENTS.md` 禁止事项 |
| `wenzhen-lore/source/README.md` 的旧相对路径 | MOVE | Task 5 已按分类机械替换完毕：原文 2 行→`source/...`、游戏整理 3 行→`game/...`、研究 2 行→`lore/research/...` | 关闭 | `MIGRATION.md` Source Namespace Mapping |
| `wenzhen-lore/source/chapter-index.md` 的旧相对路径 | MOVE | Task 5 已替换完毕：读书笔记前缀→`game/...`（11 处），无前缀简写不动 | 关闭 | `MIGRATION.md` Source Namespace Mapping |
| `lore/wiki/README.md` 小说正文示例指向 `game/` | REVIEW | 已闭合（Task 8 裁定）：`game/分支：六卷精编版/蛊真人-clean.txt` 经核对在 Git 树与磁盘上均不存在（Task 4 已从历史过滤，Task 7 只把正文放入本地 `source/`），属悬空引用；按 Source Namespace Mapping「原文→`source/`」机械修正 3 行（`rg` 示例 1 行 + 来源优先级 2 行）指向 `source/蛊真人-clean.txt`、`source/《人祖传》.txt`，与 Task 5 对 `source/README.md` 的同类修正一致；读书笔记行与 `game/docs/lore/canon-index.md` 行经核对存在，未动 | 关闭；公开克隆缺 `source/` 时按映射记 `LOCAL_ONLY` | Task 5 机械替换记录；计划 Task 7 Step 1 |
| 完整原文候选（`蛊真人-clean.txt`、`蛊真人.txt`、`《人祖传》.txt` 及同义改名） | EXCLUDE | 工作树与远程历史中存在完整原文候选；公开树不得包含。**原记录“Task 3 已审计：`git ls-files` 全树无跟踪记录”不成立（2026-09-19 复测）**：仓库根的 `蛊真人.txt` 与 `蛊真人-clean.txt` 曾被跟踪并已推送至 `origin/main`，见本表 P0 行 | 已执行（当前树止血）：`git rm --cached` 两份根原文，本地磁盘副本保留，根 `.gitignore` 三条规则改为未锚定。未执行：`git-filter-repo` 历史清理——该操作影响 `main`／`origin/main`／两条 `codex/phase2-*`／`origin/workflow-refactor`，须用户单独裁定并在隔离克隆中演练。本地只保留一份 `source/` 副本的目标改由 P0 行继续跟踪 | `CONSTRAINTS-V2` §2：不复制原著正文（法律边界）；2026-09-19 用户裁定；本表 P0 行 |
| `**/.git-nested-backup/` | EXCLUDE | 子项目历史 Git 元数据的本地备份 | 保留在本地作恢复材料；不进入 Git 树（根规则已忽略） | 计划 Global Constraints：远程 `.git` 不进入 Monorepo |
| `**/.worktrees/` | EXCLUDE | 本地工作副本 | 保留在本地；不进入 Git 树（根规则已忽略） | 计划 Global Constraints：worktree 不进入 Monorepo |
| `map_test_result.txt`、`**/*.bak-*`、`**/gate_sabotage.log`、`**/generated/_shortlist_spec.txt`、`tools/_*.txt` | EXCLUDE | 本地测试输出、备份、调试日志与生成短名单 | 已被根 `.gitignore` 排除；不提交 | 计划 Task 2 Step 1：保留现有忽略规则 |
| `gu-zhenren-editor/opencode.json`（迁移后为 `game/opencode.json`）中的机器绝对路径 | REVIEW | Godot MCP 配置包含另一台机器的绝对路径 | Task 7 登记为 REVIEW，不为迁移擅自替换 MCP 配置；仅作历史配置参考 | 计划 Worker Contract：迁移不依赖该 MCP |
| `game/opencode.json` 中的机器绝对路径 | REVIEW | Task 7 已核对：`game/opencode.json` 仍含另一台机器的 Godot MCP 绝对路径（`--project C:/Users/90877/work_space/gu-zhenrens-pigeon-meat`，`GODOT_PATH` 指向同机器 WinGet 路径）；迁移保持字节原样，未替换 | 不为迁移擅自替换 MCP 配置；是否更新由用户决定 | 计划 Worker Contract：迁移不依赖该 MCP |
| `game/tools/_*.txt`（本地测试输出，随导入历史入库） | KEEP | `game/tools/` 下 10 个 `_*.txt` 随过滤后 Gitee 历史进入 Git 树（导入提交 `ebb7f81` 内既有内容）；根 `.gitignore` Task 7 新增 `game/tools/_*.txt` 只阻止未来新增 | 保留上游历史原样，不改写、不删除 | 计划 Global Constraints：不重写导入历史 |
| `gu-zu` 中名为 `gitee` 的远程指向编辑器项目 | REVIEW | Task 3 已核对本地证据：`gu-zu/.git-nested-backup/config` 含 `[remote "gitee"] url = https://gitee.com/chen-dong-s/gu-zhenrens-pigeon-meat.git`；工作树内无嵌套 `.git`，不存在重复导入入口 | Task 5/7 只从各自镜像单源 subtree 导入，不把该远程当作第三个资料仓库；导入前复核镜像来源 URL | 备份配置实测；计划 Target Mapping（单源导入） |
| 多副本原文 SHA-256 未比对 | REVIEW | 已闭合：`source/蛊真人-clean.txt`（`95cd0b13…`，23,172,557 字节）与 `source/《人祖传》.txt`（`acc3ec34…`，151,392 字节）已建立（Task 7 Step 1），被根 `/source/` 忽略，零 CR；两份候选剥离 `\r` 后 SHA-256 相同（`.worktrees/` 副本 raw `bf78d414…` / `e6a6a618…`，`tr -d '\r'` 后与 `source/` 一致），游戏侧 LF 版本被采用 | 关闭；本地只保留这一份 `source/` 副本，不再二选一。**2026-09-19 复核补充**：该 `source/` 目录当前在磁盘上不存在（Wiki 的 `source:source/蛊真人-clean.txt` 因此记 local-only WARN），蒸馏实际读取仓库根副本；根副本已脱离 Git 跟踪但仍留在磁盘，与“只保留一份”的目标不一致，收敛到唯一 `source/` 副本另立任务，见 P0 行 | 计划 Worker Contract 行为契约 5；Task 7 实测 SHA（见 `MIGRATION.md` Task 7 小节）；2026-09-19 P0 复测 |
| 父仓库 `origin` URL 形如 `https://github.com/Zachar-c/-.git` | REVIEW | Task 3 实测 `git remote -v` 双方均为该值；本次任务不推送，不影响审计与基线登记 | Task 9 推送前由用户核对远端地址与分支策略；worker 不自动推送、不改远端配置 | 实测输出；计划 Task 9（用户确认后推送） |
| `docs/superpowers/plans/2026-09-18-wenzhen-monorepo-migration.md` | ARCHIVE | 主张“保留旧根目录结构”的旧迁移计划，已被本计划取代 | Task 2 仅加历史说明头指向本计划，不再执行其方案 | 计划 Task 2 Step 4 |
| `docs/superpowers/specs/2026-09-18-gu-zhenren-monorepo-migration-design.md` | KEEP | 当前迁移设计依据（v1.0，待审阅） | 作为 Task 2—8 的边界依据保留 | 设计 §1—§10 |
| `game/docs/contracts/2026-09-12-agent-ownership-contract.md`（迁移后路径） | ARCHIVE | 已被 `CONSTRAINTS-V2` 明确作废的旧协作契约 | 保留作历史参考，不作为执行契约 | `CONSTRAINTS-V2` §1 F 组；计划 Global Constraints |
| 旧路径本地残留（`gu-zhenren-editor/` 磁盘未跟踪文件约 4500 个） | EXCLUDE | Task 7 `git rm` 只删除受版本文件；嵌套 `.gitignore` 离树后本地缓存（`.godot/`、`.workbuddy/` 等）显形为 `?? gu-zhenren-editor/`；Task 8 根 `.gitignore` 新增 `/gu-zhenren-editor/` 整目录忽略（既有 `分支：六卷精编版/...` 与 `tools/_*.txt` 两条保留作纵深防御），`git status` 已干净，`git ls-files` 仍为 0 | 保留在磁盘作本地恢复材料；不进入 Git 树；worker 不 `git clean`、不删除 | Task 8 实测；`MIGRATION.md` Task 8 小节 |
| `game/wenzhen-web/assets/*.import`（9 个本地 Godot 生成文件，未跟踪） | KEEP | Task 8 发现：`db34873` 带过的本地 asset（9 个 png）缺 `.import` 侧车文件，本地 Godot 运行后生成；本项目惯例跟踪 `.import`（全树 221 个），故不加宽泛忽略、不删除、不代用户提交 | 用户 2026-09-18 裁定入库，随提交 `1d8a7b3` 提交；逐文件仅含 `res://` 路径与 `uid`，无机器绝对路径；与全树 221 个已跟踪 `.import` 惯例一致 | Task 8 实测；`MIGRATION.md` Task 8 小节 |
| `lore/research/分支：六卷精编版/读书笔记/` 与 `game/分支：六卷精编版/读书笔记/` 双份拷贝 | REVIEW | 2026-09-18 PR 评审发现：两侧为同批 22 个文件，逐行内容一致、仅行尾不同（research 侧 CRLF、game 侧 LF，字节差正好等于行数）；`lore/wiki/` frontmatter 混引两处（`notes:` 各 25 处），阅读者无法判断 canonical | 现在不移动、不合并，避免产生新 diff 影响 Wiki 稳定性；未来单独立项「research source consolidation」并选定唯一引用前缀 | 2026-09-18 PR 评审（列为最高优先级债务）；本轮实测 SHA-256 全量差异 + `Compare-Object` 逐行比对为 0 差异 |
| `lore/wiki/tools/check.ps1` check2（来源路径存在） | REVIEW | check2 要求 frontmatter 的 `source:`/`notes:`/`memory:` 目标在本地存在；`source/` 被根 `.gitignore` 排除，干净克隆上必然缺失。已知历史 local-only 路径仅两条：`source/蛊真人-clean.txt`、`source/《人祖传》.txt`（根 AGENTS.md 约束不进入 Git），本地缺失时记 WARN（不计入 failures）；其他任何缺失的 source:/notes:/memory: 路径仍记 FAIL | 已收口：check2 汇总显示 PASS/WARN/FAIL 数；仅两条已知路径缺失时整套脚本 exit 0，出现未知缺失仍 exit 1；不把全部 source/ 缺失泛化成 WARN | 2026-09-18 PR 评审；2026-09-18 来源边界收口；`check.ps1` 实测 |