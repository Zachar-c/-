# 2026-09-07 交接文档

> 面向下一个接手会话。读完这份就能继续，不需要翻历史对话。
> 并行会话同步另有 `docs/superpowers/plans/2026-09-07-repo-hygiene-data-driven-tests.md`（三工单：根目录收敛 / 数据驱动 / 核心测试），**动域层或工单前先读它**。

## 0. 当前状态

| 项 | 值 |
|---|---|
| 分支 | `master`（与 `origin/master` 同步，最新远端 `f13f561`） |
| unit | **1130 / 1130 全绿**（34584 asserts，约 112s） |
| integration | **31 / 31 全绿**（1253 asserts，约 110s） |
| `tools/check.ps1` | exit 0 |
| 工作树 | **有 11 个未提交改动**（视觉会话产物，见 §3） |

## 1. 今天完成的事

1. **红测清零**：唯一红测 `test_slice_buffs::test_lesser_one_hp_spares_bosses_only`（期望 Boss hp=8、实得 14）。根因是 `a257bd5` 把 `data/enemies.json` 的 `miasma_vein_lord.hp` 8→14，测试硬编码未同步。已把断言改成**数据驱动**（从 `controller.catalog["enemies"]` 取基线，断言 `hp == 基线` 且 `> 1`）。
2. **旧结论作废**：此前记录的「unit 1096/1098，2 个失败为 `test_wenzhen_card_fsm` 顺序依赖」**已不复现**，隔离 10/10、全量 10/10 均绿。别再按旧结论排查。
3. **数据侧定性**：`a257bd5` 不是误改，是**体系化 Boss 强化**（hp 8/8/9/10/11 → 14/15/16/18/20，essence +2，意图伤害 +1）。方向是「断言对齐数据」，**不要回滚数据**。
4. **全库硬编码扫描**（工单 2 交付）：域层已基本数据驱动，9 处命中 = 5 误报 + 2 死代码 + 2 真候选。详见 §4。
5. **技能迭代**：`ponytail` 第 2 级阶梯补「写文件前先读」；`godot-gut-verify` 补维护记录。

## 2. 验证命令（照抄即可）

Godot 在 `C:/Users/90877/AppData/Local/Microsoft/WinGet/Packages/GodotEngine.GodotEngine_Microsoft.Winget.Source_8wekyb3d8bbwe/Godot_v4.7.2-stable_win64_console.exe`（`tools/godot.ps1` 无参不输出路径，别依赖）。

```bash
# 单文件（快，前台）
"$GODOT" --headless --path . -s addons/gut/gut_cmdln.gd -gtest=res://tests/unit/X.gd -gexit

# 全量（约 2 分钟，必须用后台，前台 120s 会被 SIGTERM）
"$GODOT" --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gexit

# 噪音过滤（headless 会刷大量 image WARNING + backtrace）
... 2>&1 | grep -v "^WARNING\|^   at:\|GDScript backtrace\|^       \[" | tail -30
```

推送挂起时用 wincred 直读（agent 会话里 credential-manager 会等 UI 挂死）：

```bash
git -c credential.helper= -c credential.helper="C:/Users/90877/.workbuddy/binaries/PortableGit/versions/1.2.0/mingw64/bin/git-credential-wincred.exe" push origin master
git ls-remote origin refs/heads/master   # 落地校验以它为准
```

## 3. 未提交的工作树改动（**风险项**）

```
 M docs/contracts/2026-09-02-domain-ui-contract.md
 M scenes/ui/screens/hall_screen.tscn
 M scenes/ui/screens/map_screen.tscn
 M scripts/presentation/gu_style.gd
 M scripts/presentation/run_snapshot_builder.gd
 M scripts/presentation/screens/hall_screen_view.gd
 M scripts/presentation/screens/map_screen_view.gd
 M scripts/presentation/wenzhen_master_theme.gd
 M tests/unit/test_v3_title_screen.gd
 M tests/unit/test_wenzhen_hall_screen.gd
 M tests/unit/test_wenzhen_map_screen.gd
?? assets/wenzhen/hall/hall_dots.png(.import)
?? docs/superpowers/specs/2026-09-07-*-wireframe.html（3 个）
?? tools/debug_map_geometry.gd
?? tools/verify_hall_v8_render.gd
?? tools/verify_map_v1_render.gd
?? .preview/  .superpowers/
```

**为什么这是风险**：本仓库已发生 4 次「并行 visual/audio 会话整目录物理删除」事故，且声明停止后清理进程仍可能残留执行。这批改动目前**只存在于本地工作树**，没有远端副本。建议尽早打 checkpoint 提交（提交时按 AGENTS 第 8 条排除 `.claude/`、未验证的 `data/enemies.json`、`data/dialogues/events.dialogue`、截图目录与 `__pycache__`）。

## 4. 工单 2 扫描结论（数据驱动）

方法：`grep -rnE "(damage|heal|cost|bonus|value|amount|threshold|cap|multiplier|ratio|weight|pct|stacks)\"?\s*[:=]\s*-?[0-9]+" scripts/domain/*.gd`

**蛊 / 敌人 / 道具 / 事件四类实体的硬编码残留 = 0。**

- **误报（不动）**：`battle_command_facade.gd:75`（中性倍率 1.0）、`economy_rules.gd:86`（/100.0 换算）、`death_report_builder.gd:16` / `synthesis_rules.gd:44` / `action_resolver.gd:50`（0 兜底）。
- **死代码（不迁）**：`scripts/domain/school_rules.gd` 近乎整文件 —— `overchannel_benefit`、`apply_overchannel_soul` 零调用；`blood_stacks` 三函数与 `material_fuel` 只被自己的 `test_school_framework.gd:81-92` 引用。
- **真候选（仅 2 处）**：

| 位置 | 内容 | 性质 |
|---|---|---|
| `v1_battle_resolver.gd:23-28` | 基础动作表 attack 2 / defense 3 / heal 2 / shift 1 / marked 1 / bound 1 | 活 + 核心 + **高风险**，先补测试再迁 |
| `blood_qi_rules.gd:41` | time_cost 1、blood_trail 1 | 活 + 单处 + **低成本** |

- **改判**：原计划「`overchannel_benefit` → 抽 `data/schools.json`」**取消**。`schools.json` 每校目前只有 `name/summary/starter_gu_ids`，唯一候选是死代码，不要为死代码做迁移。

## 5. 未决事项（按优先级）

| # | 事项 | 说明 |
|---|---|---|
| P1 | **提交 checkpoint** | §3 那批未提交改动，防并行会话删除事故重演 |
| P2 | 迁 `blood_qi_rules.gd:41` | 便宜、安全、立刻见效 |
| P3 | 迁 `v1_battle_resolver.gd:23-28` | 先补测试再迁，别裸迁核心 |
| P4 | 一行 grep 守门 | 加进 `tools/check.ps1`：同正则 + 4 处误报白名单，命中即报警 |
| P5 | `school_rules.gd` 并入 B1 删除工单 | 连同 `test_school_framework.gd` 对应用例；`is_soul` 尚有 1 处外部引用待确认 |
| ~~P6~~ | ✅ **已闭环**（2026-09-08 凌晨） | 玩家反馈「重复打怪、掉落少、商店/休整/炼蛊点少、玩不到 Boss」。已改 `data/pacing.json`（纯数据）：黑市 1→2 个/层、五层都补炼蛊点、每层战斗模板 1→3 种、`material_count` +1。连带把 3 个测试的黑市/材料数硬编码改为从 pacing 读取。loot+map+catalog **61/61 全绿**。详见 `.workbuddy/memory/2026-09-08.md` |
| ~~P7~~ | ⚠️ **仍待你提供 GitHub URL** | 已排查：gitee 两分支+全量历史无此文件、推荐市场 0 结果、GitHub API `total_count=0`、本地 blobs/changes-detail 只有我今天写入的内容。**原稿无法自动恢复** |

## 6. 事故与约定（务必遵守）

- **我丢失了用户自写的技能文件**：`~/.workbuddy/skills/godot-gut-verify/SKILL.md` 被一次「未读先写」的 Write 覆盖，**原内容无法恢复**（无备份、不在 git、历史对话检索为空）。当前内容是当日实测重写的版本。**若你有原稿，交出来合并，别直接覆盖回去**（会丢掉新增的 GUT 跑法与数据驱动断言规则）。
- **Write 前先 Read**：已写进 `ponytail` 第 2 级阶梯。
- **测试断言不硬编码数值基线**，一律从 catalog 取（首个范式：`test_slice_buffs.gd`）。
- `~/.workbuddy/` 不在任何版本控制下 —— 建议把 `skills/` 单独 `git init`。
- 技能管理偏好：**先迭代已有 skill，覆盖不到才新建**。

## 7. 建议的下一步顺序

P1（提交）→ P2（`blood_qi_rules`）→ P3（V1 动作表，先补测）→ P4（守门）→ P5（并入 B1）。
P6 / P7 需要你本人拍板。
